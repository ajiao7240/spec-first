# 角色访谈记录

`acquisition_id: team-standards-v2-pilot-20260623`

本次 V2 pilot 没有真实角色访谈输入，因此所有角色状态为 `not-run`。不要为缺席角色补写答案；缺口进入 limitations 或 open questions。

| 角色 | 状态 | 候选影响 | Open question | Next action |
| --- | --- | --- | --- | --- |
| architecture owner | `not-run` | none | 是否要把 acquisition run 机制提升为 confirmed standards rule? | 仅在请求 promotion 时执行 `owner-review` |
| security/privacy owner | `not-run` | none | 是否需要更严格的 redaction policy for replay fixtures? | `collect-more-evidence` |
| test/QA owner | `not-run` | none | 是否有可复现 PR/review replay 样本? | `collect-more-evidence` |
| SRE/operations owner | `not-run` | none | 是否有 incident-derived standards 获取需求? | `defer` |
| App/H5/PC/Admin owner | `not-run` | none | 是否存在 cross-surface standards pilot? | `defer` |
| Backend/Data owner | `not-run` | none | 是否存在 backend/data standards pilot? | `defer` |
| product/business owner | `not-run` | none | 是否存在业务 owner 授权规则? | `defer` |

本 pilot 没有记录 owner decision。
