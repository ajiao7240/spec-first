# spec-first 终版规划

## 从 Spec-Driven Workflow 升级为 AI Coding Harness

这份规划按 **spec-first 开源维护者** 视角制定。核心目标不是继续堆 prompt、skill、agent，而是把 spec-first 打造成一套完整的：

```text
repo-local, evidence-governed, review-closed AI coding Harness
```

也就是帮助用户把 AI 辅助编码从：

```text
一次性聊天 / vibe coding / 临时 prompt / 单点 agent 执行
```

升级为：

```text
需求有锚点
上下文有路由
计划有证据
任务可交接
执行有边界
审查有闭环
知识可复用
团队可治理
```

---

# 1. 终局定位

## 1.1 一句话定位

```text
spec-first 是一套仓库本地的 AI Coding Harness，
用于把 Claude Code、Codex、Copilot、Gemini CLI、OpenCode 等 AI coding agent
纳入可控、可证、可审、可复用的工程闭环。
```

英文定位：

```text
spec-first is a repo-local Harness for evidence-governed AI-assisted software engineering.
```

## 1.2 spec-first 不是什么

```text
不是 another coding agent
不是 prompt collection
不是 slash command collection
不是 Spec Kit clone
不是 IDE
不是 SaaS 平台
不是重 workflow engine
不是 agent marketplace
```

## 1.3 spec-first 是什么

```text
Workflow Harness
+ Evidence Governance
+ Context Routing
+ Provider Readiness
+ Task-bound Execution
+ Review Closure
+ Knowledge Compound
+ Skill Governance
```

最终产品心智：

```text
Spec 是入口。
Graph 是证据。
Plan 是决策。
Task 是交接。
Work 是受控执行。
Review 是质量闭环。
Compound 是知识复利。
```

---

# 2. 业界趋势判断

当前业界已经从“模型会不会写代码”进入“谁能把 agent 组织进真实工程系统”的阶段。

GitHub Spec Kit 已经把 Spec-Driven Development 定义为以 specification 为中心的 AI-assisted software development 方法：先定义要构建什么，再通过结构化阶段细化，并交给任意 AI coding agent 实现。这个趋势证明 `Spec > Code` 方向成立，但也意味着 spec-first 不能停在“spec 模板”，必须往完整 Harness 走。([github.github.com][1])

OpenAI Codex Skills 官方把 skill 定义成可复用能力包，并通过 progressive disclosure 管理上下文：模型先看到 name、description、path，只有决定使用时才加载完整 `SKILL.md`。这说明未来 skill 不是 prompt，而是 instructions + resources + scripts + context budget 的工程能力单元。([OpenAI 开发者][2])

Anthropic 在 MCP code execution 文章里指出，agent 连接数百甚至上千工具时，如果把所有工具定义和中间结果都塞进上下文，会增加成本、延迟并让流程变脆。这直接说明未来必须做 tool/context routing，而不是无脑扩 MCP。([Anthropic][3])

Anthropic 对 long-running agents 和 Managed Agents 的讨论也说明，未来 agent 会做更长周期、更复杂的工作，而 harness、context management、sandbox、工具抽象和可恢复机制会成为核心能力。([Anthropic][4])

GitHub Copilot coding agent 已经在强化 model picker、self-review、安全扫描、custom agents、CLI handoff；OpenAI Codex 也在往端到端工程任务、代码审查、并行 threads、worktrees、automations、cloud background tasks 发展。这说明 agent runtime 会越来越强，spec-first 不应该重复造 agent，而应该做 agent 之上的工程控制层。([The GitHub Blog][5])

所以未来 12–24 个月的核心趋势是：

```text
Agent Runtime 会商品化。
Skills / Plugins 会能力包化。
MCP / Tools 会爆炸。
Context Engineering 会成为基础设施。
Review / Verification 会成为质量瓶颈。
Long-running / multi-agent 会变成常态。
企业会从个人 AI 工具试用转向 AI-native engineering governance。
```

spec-first 必须提前解决未来问题，而不仅是当下问题。

---

# 3. 总体战略

## 3.1 战略目标

```text
把 spec-first 从 spec-driven workflow collection
升级为 evidence-governed AI coding Harness。
```

## 3.2 战略路径

```text
先统一定位
再建立协议
再打通证据
再路由上下文
再闭合 review
再支持长任务
再沉淀知识
再扩展能力生态
最后进入团队治理
```

## 3.3 核心原则

```text
1. Repo-local first
2. Evidence-first
3. Review-closed
4. Context-budgeted
5. Tool-risk-aware
6. Skill-contract-based
7. Provider-replaceable
8. Host-agnostic
9. Light contract, not heavy workflow engine
10. Scripts prepare, LLM decides
```

## 3.4 不做原则

```text
不和 Claude Code / Codex / Copilot 抢 agent runtime。
不做重状态机。
不做中心化 SaaS。
不做无边界 skill/agent 大杂烩。
不把 MCP 工具越接越多当成能力升级。
不把未验证猜测沉淀成知识。
不让 agent 越权成为最终裁判。
```

---

# 4. 终版架构

## 4.1 总体架构图

```text
┌──────────────────────────────────────────────┐
│              AI Coding Hosts                 │
│ Claude Code / Codex / Copilot / Gemini / etc │
└──────────────────────┬───────────────────────┘
                       │
┌──────────────────────▼───────────────────────┐
│          spec-first Workflow Harness          │
│ brainstorm → plan → tasks → work → review    │
└──────────────────────┬───────────────────────┘
                       │
┌──────────────────────▼───────────────────────┐
│            Artifact Protocol Layer            │
│ requirements / plan / task-pack / findings   │
└──────────────────────┬───────────────────────┘
                       │
┌──────────────────────▼───────────────────────┐
│           Evidence Governance Layer           │
│ evidence packet / claims / risks / unknowns  │
└──────────────────────┬───────────────────────┘
                       │
┌──────────────────────▼───────────────────────┐
│            Context + Tool Routing Layer       │
│ graph / diff / tests / standards / tool risk │
└──────────────────────┬───────────────────────┘
                       │
┌──────────────────────▼───────────────────────┐
│            Provider Adapter Layer             │
│ GitNexus / Serena / ast-grep / git / MCP      │
└──────────────────────┬───────────────────────┘
                       │
┌──────────────────────▼───────────────────────┐
│            Repo-local Engineering Memory      │
│ .spec-first / docs / reviews / compound       │
└──────────────────────────────────────────────┘
```

## 4.2 七层能力

| 层                      | 目标           | 核心产物                                       |
| ---------------------- | ------------ | ------------------------------------------ |
| Workflow Harness       | 组织研发阶段       | stage-contract、handoff                     |
| Artifact Protocol      | 稳定人机可读产物     | artifact-header、task-pack                  |
| Evidence Governance    | 让判断可追溯       | evidence-packet、claim、risk                 |
| Context + Tool Routing | 路由最小充分上下文和工具 | context-request、context-bundle、tool-budget |
| Provider Adapter       | 统一接入外部能力     | provider-artifacts、runtime-capabilities    |
| Review Closure         | 审查、修复、复审、沉淀  | review-findings、fix-plan、re-review         |
| Knowledge Compound     | 沉淀项目知识复利     | compound-delta、repo-profile、glue-map       |

---

# 5. Repo-local 目录终版

建议将 `.spec-first/` 定义为 Harness 的核心工作目录。

```text
.spec-first/
  config/
    runtime-capabilities.json
    graph-providers.json
    provider-artifacts.json
    workflow-profile.json
    capability-registry.json
    tool-risk-policy.json

  graph/
    graph-facts.json
    graph-index-status.json
    bootstrap-impact-capabilities.json
    code-navigation.json
    graph-operations.jsonl

  specs/
    2026-05-13-001-feature-name/
      requirements.md
      plan.md
      task-pack.yaml
      evidence-packet.json
      context-request.json
      context-bundle.json
      review-report.md
      review-findings.json
      fix-plan.md
      re-review-result.md
      compound-delta.yaml

  reviews/
    runs/
      <run-id>/
        review-input.json
        reviewer-results/
        synthesis.md
        fix-plan.md
        residual-risks.md

  runs/
    <run-id>/
      run-state.json
      task-state.json
      evidence-snapshot.json
      context-snapshot.json
      decision-log.md
      resume-instruction.md
      agent-activity.jsonl

  standards/
    project-shape.json
    standards-plan.json
    glue-map.json
    standards-candidates.json
    standards-preview.md
    repo-profile.yaml

  compound/
    patterns/
    failures/
    decisions/
    conventions/
```

原则：

```text
docs/ 给人读。
.spec-first/ 给 workflow、agent、review、resume、compound 读。
```

---

# 6. 核心协议终版

## 6.1 Stage Contract

每个核心 skill 必须有 `contract.yaml`。

```yaml
schema_version: spec-first.stage-contract.v1
stage_id: plan
stage_type: planning

inputs:
  required:
    - requirements_packet
    - graph_readiness
  optional:
    - standards_preview
    - repo_profile
    - prior_compound_notes

outputs:
  required:
    - plan
    - evidence_packet
  optional:
    - risk_register
    - open_questions

rules:
  - do_not_write_code
  - cite_evidence_for_architecture_claims
  - mark_unknowns_explicitly

handoff:
  next:
    - write-tasks
```

覆盖范围：

```text
spec-mcp-setup
spec-graph-bootstrap
spec-brainstorm
spec-doc-review
spec-plan
spec-write-tasks
spec-work
spec-debug
spec-optimize
spec-polish
spec-code-review
spec-app-consistency-audit
spec-compound
spec-compound-refresh
spec-sessions
spec-skill-audit
```

---

## 6.2 Artifact Header

所有核心 markdown 产物必须有统一头。

```yaml
---
schema_version: spec-first.artifact.v1
artifact_type: plan
spec_id: 2026-05-13-001-feature
stage: plan
source_stage: brainstorm
repo_commit: abc123
created_by: spec-plan
evidence_packet: .spec-first/specs/2026-05-13-001-feature/evidence-packet.json
status: draft
---
```

---

## 6.3 Evidence Packet

所有关键阶段都要生成或更新 `evidence-packet.json`。

```json
{
  "schema_version": "spec-first.evidence-packet.v1",
  "spec_id": "2026-05-13-001-feature",
  "stage": "plan",
  "repo": {
    "branch": "feature/demo",
    "commit": "abc123"
  },
  "sources": [],
  "claims": [],
  "unknowns": [],
  "risks": []
}
```

证据链标准：

```text
claim → evidence → decision → task → diff → review → compound
```

铁律：

```text
没有 evidence 的结论必须标 assumption。
没有 task-pack 不进入 work。
没有 diff/context/task-pack 不进入 code-review。
没有 review closure 不进入 compound。
```

---

## 6.4 Task Pack

```yaml
schema_version: spec-first.task-pack.v1
spec_id: 2026-05-13-001-feature

tasks:
  - id: T-001
    title: 增加 runtime-capabilities.json 生成逻辑
    source_requirements:
      - R-001
    evidence:
      - CLAIM-001
    expected_files:
      - skills/spec-mcp-setup/scripts/check-health
    acceptance:
      - 运行 /spec:mcp-setup 后生成 runtime-capabilities.json
      - 缺少必需 helper 时 baseline_ready=false
    risk:
      level: medium
      reason: 会影响 graph-bootstrap readiness 判断
    review_focus:
      - provider readiness
      - cross-platform path handling
```

---

## 6.5 Context Request

```json
{
  "schema_version": "spec-first.context-request.v1",
  "stage": "code-review",
  "intent": "review_diff_for_regression_risk",
  "changed_files": [],
  "needs": [
    "direct_dependencies",
    "callers",
    "tests",
    "standards",
    "prior_failures"
  ],
  "budget": {
    "max_files": 20,
    "max_tokens": 60000,
    "prefer_symbols": true
  }
}
```

---

## 6.6 Context Bundle

```json
{
  "schema_version": "spec-first.context-bundle.v1",
  "providers_used": [],
  "included_context": [],
  "excluded_context": [],
  "tool_risk": [],
  "confidence": "medium"
}
```

Context Bundle 必须说明：

```text
为什么包含这些上下文。
为什么排除这些上下文。
用了哪些 provider。
哪些 provider degraded。
本次 token/tool budget 如何控制。
```

---

## 6.7 Review Finding

```json
{
  "schema_version": "spec-first.review-finding.v1",
  "finding_id": "F-001",
  "severity": "blocking",
  "category": "regression-risk",
  "title": "缺少失败分支回归测试",
  "description": "本次修改改变了失败分支，但没有测试覆盖。",
  "evidence": [],
  "recommendation": "补充失败分支测试，并重新运行相关测试。",
  "requires_changelog": true
}
```

---

## 6.8 Compound Delta

```yaml
schema_version: spec-first.compound-delta.v1
spec_id: 2026-05-13-001-feature

new_patterns: []
new_failures: []
new_decisions: []
standards_candidates: []
```

只沉淀：

```text
confirmed pattern
confirmed failure
confirmed decision
confirmed convention
```

不沉淀：

```text
LLM 猜测
泛泛最佳实践
一次性聊天总结
未验证建议
```

---

# 7. 版本路线总览

终版路线：

```text
vNext-0  Product Truth Release
vNext-1  Harness Contract Release
vNext-2  Evidence & Graph Readiness Release
vNext-3  Context + Tool Routing Release
vNext-4  Review Closure Release
vNext-5  Long-running Sessions Release
vNext-6  Knowledge Compound + Standards Release
vNext-7  Capability Pack + Ecosystem Release
vNext-8  Team Governance + Metrics Release
v1.0     Stable AI Coding Harness
```

---

# 8. vNext-0：Product Truth Release

## 目标

把项目定位讲清楚。

```text
spec-first = AI coding Harness
```

## 交付

```text
README 重写
README.zh-CN 重写
官网首页重写
CLI help 重写
docs/architecture/harness-architecture.md
Quick Start 重写
完整 demo repo
```

## 验收标准

用户 3 分钟内能理解：

```text
spec-first 是什么
解决什么问题
为什么不是 coding agent
为什么不是 Spec Kit clone
完整闭环怎么跑
每一步产物是什么
```

## 不做

```text
不新增 skill
不接 ECC
不做 workspace
不做 dashboard
```

---

# 9. vNext-1：Harness Contract Release

## 目标

把 skill 从“命令入口”升级为“研发阶段契约”。

## 交付

```text
contracts/
  stage-contract.schema.json
  artifact-header.schema.json
  requirements-packet.schema.json
  task-pack.schema.json
  review-finding.schema.json
  compound-delta.schema.json
  capability-manifest.schema.json
```

核心 skill 增加：

```text
SKILL.md
contract.yaml
scripts/
references/
examples/
tests/
```

## 需要改造的 skill

```text
spec-brainstorm
spec-plan
spec-write-tasks
spec-work
spec-code-review
spec-compound
spec-graph-bootstrap
spec-mcp-setup
```

## 验收标准

每个核心 skill 能回答：

```text
我读什么？
我产出什么？
我不能做什么？
我交给谁？
我的 evidence 从哪里来？
```

---

# 10. vNext-2：Evidence & Graph Readiness Release

## 目标

让 plan、tasks、work、review 不再靠猜。

## 交付

```text
.spec-first/config/runtime-capabilities.json
.spec-first/config/provider-artifacts.json
.spec-first/config/graph-providers.json

.spec-first/graph/graph-facts.json
.spec-first/graph/graph-index-status.json
.spec-first/graph/bootstrap-impact-capabilities.json
.spec-first/graph/code-navigation.json
.spec-first/graph/graph-operations.jsonl
```

## Graph Readiness Compiler

`spec-graph-bootstrap` 正式升级为：

```text
Graph Readiness Compiler
```

## query_ready 铁律

```text
query_ready=true 必须代表 query surface 真实可用。
```

不能只看：

```text
工具安装了
索引存在了
命令 exit 0
```

必须验证：

```text
索引与当前 commit 对齐
smoke query 成功
stderr 无 provider error
empty result 是合理为空而不是异常
```

## 验收标准

`plan / write-tasks / code-review` 都能读取 graph readiness 和 evidence packet。

---

# 11. vNext-3：Context + Tool Routing Release

## 目标

从“上下文堆叠”升级为“上下文和工具路由”。

## 交付

```text
context-request.v1
context-bundle.v1
tool-budget.v1
provider-risk.v1
context-router
provider fallback
```

## Context Router 管两件事

```text
1. Context Routing
当前任务该读哪些代码、文档、测试、graph、standards？

2. Tool Routing
当前任务该暴露哪些工具、MCP、provider、权限？
```

## Provider 分层

```text
GitNexus：全局代码图谱
code-review-graph：增量影响分析
Serena：符号级检索 / 编辑
ast-grep：结构搜索 fallback
ripgrep：文本搜索 fallback
git：diff / history
standards：repo-profile / glue-map / compound
```

## 验收标准

code-review 能输出：

```text
included context
excluded context
providers used
provider confidence
tool risk
token budget
```

---

# 12. vNext-4：Review Closure Release

## 目标

把 `/spec:code-review` 做成 spec-first 的王牌能力。

## 流程

```text
diff
→ task-pack
→ evidence-packet
→ context-bundle
→ selective reviewer dispatch
→ evidence audit
→ synthesis
→ fix-plan
→ re-review
→ compound
```

## 交付

```text
review-input.json
review-findings.json
synthesis.md
fix-plan.md
re-review-result.md
residual-risks.md
compound-candidates.md
```

## reviewer dispatch

| 改动类型        | 调用专家                                                   |
| ----------- | ------------------------------------------------------ |
| 普通代码        | Code Quality Reviewer、Test Reviewer                    |
| 架构变化        | Architecture Reviewer、Evidence Auditor                 |
| CLI/跨平台     | CLI Reliability Reviewer、Shell Compatibility Reviewer  |
| 移动端/KMP     | Mobile UX、KMP Architect、I18n、Analytics                 |
| 安全权限        | Security Reviewer、Regression Reviewer                  |
| 文档官网        | Docs Consistency Reviewer、Product Positioning Reviewer |
| 图谱/Provider | Graph Readiness Reviewer、Provider Adapter Reviewer     |

## 验收 fixture

必须能发现：

```text
缺少 CHANGELOG
缺少测试
query_ready 假阳性
跨平台路径 bug
需求偏离
影响面遗漏
安全风险
文档代码不一致
provider degraded 未标注
```

---

# 13. vNext-5：Long-running Sessions Release

## 目标

支持未来长任务、多 agent、可恢复执行。

## 交付

```text
.spec-first/runs/<run-id>/
  run-state.json
  task-state.json
  evidence-snapshot.json
  context-snapshot.json
  decision-log.md
  resume-instruction.md
  agent-activity.jsonl
```

## 解决的问题

```text
任务中断后怎么恢复？
多个 agent 并行后怎么合并？
上次为什么这么决策？
哪些问题还没解决？
当前 evidence 是否过期？
```

## 验收标准

一次任务中断后，可以恢复：

```text
/spec:sessions resume <run-id>
```

并且 AI 能明确知道：

```text
上次做到哪
哪些任务完成了
哪些任务阻塞
下一步该做什么
不能重复做什么
```

---

# 14. vNext-6：Knowledge Compound + Standards Release

## 目标

让每次 AI 辅助研发都变成项目知识资产。

## 交付

```text
compound-delta.v1
repo-profile.yaml MVP
standards-preview.md
glue-map.json
confirmed-patterns.md
confirmed-failures.md
confirmed-decisions.md
```

## spec-standards 保持轻量

推荐闭环：

```text
prepare-baseline.js
→ project-shape.json
→ standards-plan.json
→ glue-map.json
→ standards-candidates.json
→ standards-preview.md
→ downstream workflows read as context
```

## repo-profile MVP

```yaml
schema_version: 1
repo_id: ...
project_type: ...
languages: []
project_intent:
  summary: ...
principles: []
non_negotiables: []
review_defaults: []
```

## 验收标准

下一次 plan / code-review 能读取上一次 compound 产物，并说明：

```text
本次复用了哪些历史经验
哪些经验不适用
哪些规范只是候选
```

---

# 15. vNext-7：Capability Pack + Ecosystem Release

## 目标

面向未来 Skills / Plugins / MCP 生态，建立 spec-first 自己的 capability pack 格式。

## 交付

```text
capability-pack/
  manifest.yaml
  SKILL.md
  contract.yaml
  resources/
  scripts/
  examples/
  tests/
  source-lock.json
```

## 接入原则

```text
先 lens
再 agent
再 skill
最后才 workflow
```

铁律：

```text
能用 lens，不新增 agent。
能用 agent，不新增 skill。
能扩展现有 skill，不新增 workflow。
能 preview，不 writeback。
```

## 用途

```text
ECC lens pack
mobile audit pack
security review pack
performance review pack
domain review pack
```

## 不做

```text
不做 marketplace
不直接复制外部 command/hook
不让外部 pack 直接写用户 repo
```

---

# 16. vNext-8：Team Governance + Metrics Release

## 目标

为企业团队使用提前铺路。

## 交付

```text
ai-usage-summary.json
review-quality-summary.json
cycle-time-summary.json
agent-run-summary.json
risk-trend.md
team-adoption-report.md
```

## 指标

```text
需求到任务耗时
任务到实现耗时
review findings 数量
blocking finding 比例
re-review 次数
缺陷类型分布
compound 复用次数
AI 参与阶段分布
```

## 不做

```text
不做 SaaS
不做中心平台
不做员工绩效工具
不做“AI 替代人”叙事
```

---

# 17. v1.0：Stable AI Coding Harness

## v1.0 定义

```text
一个普通开源用户，
在真实 Git 仓库里，
可以用 spec-first 稳定完成：

需求
→ 上下文准备
→ 计划
→ 任务
→ 实现
→ 审查
→ 修复
→ 复盘
→ 知识沉淀
```

## v1.0 必须满足

```text
安装稳定
init 稳定
Claude / Codex 双 host 稳定
核心 skill 有 contract
核心 artifact 有 schema
graph readiness 可用
context bundle 可生成
review finding 结构化
fix-plan / re-review 可跑
compound 可沉淀
skill-audit 可治理
demo repo 完整闭环
跨平台 smoke test
官网 / README / CLI 叙事一致
```

## v1.0 不需要满足

```text
不需要完整多仓平台
不需要云端 dashboard
不需要自研 coding agent
不需要 marketplace
不需要重 workflow engine
```

---

# 18. P0 / P1 / P2 任务清单

## P0：地基任务

```text
P0-01 重写项目定位：spec-first = AI coding Harness
P0-02 新增 docs/architecture/harness-architecture.md
P0-03 新增 contracts/ 目录
P0-04 定义 stage-contract.v1
P0-05 定义 artifact-header.v1
P0-06 定义 evidence-packet.v1
P0-07 定义 task-pack.v1
P0-08 定义 review-finding.v1
P0-09 定义 compound-delta.v1
P0-10 为核心 skill 增加 contract.yaml
P0-11 mcp-setup 输出 runtime-capabilities.json
P0-12 mcp-setup 输出 provider-artifacts.json
P0-13 graph-bootstrap 输出 graph readiness artifacts
P0-14 修正 query_ready 真实语义
P0-15 plan 读取 graph/evidence
P0-16 write-tasks 继承 plan evidence
P0-17 code-review 消费 task-pack/evidence/diff
P0-18 compound 输出 compound-delta
P0-19 skill-audit 检查 contract/evidence/changelog
P0-20 demo repo 跑通最小闭环
```

## P1：强化任务

```text
P1-01 context-request.v1
P1-02 context-bundle.v1
P1-03 context-router
P1-04 tool-budget.v1
P1-05 provider-risk.v1
P1-06 GitNexus provider adapter
P1-07 Serena provider adapter
P1-08 ast-grep fallback
P1-09 ripgrep fallback
P1-10 selective reviewer dispatch
P1-11 Evidence Auditor
P1-12 fix-plan protocol
P1-13 re-review protocol
P1-14 review quality fixtures
P1-15 standards-preview 接入 plan/review
```

## P2：未来任务

```text
P2-01 sessions run-state
P2-02 sessions task-state
P2-03 resume-instruction
P2-04 compound knowledge index
P2-05 repo-profile MVP
P2-06 capability-pack manifest
P2-07 ECC lens 接入
P2-08 multi-module context routing
P2-09 workspace advisory artifacts
P2-10 team metrics summary
```

---

# 19. 测试与验收体系

## 19.1 Schema tests

验证：

```text
stage-contract
artifact-header
evidence-packet
task-pack
context-request
context-bundle
review-finding
compound-delta
```

## 19.2 Workflow golden tests

在 demo repo 跑：

```text
brainstorm
→ plan
→ write-tasks
→ work
→ code-review
→ compound
```

检查：

```text
产物存在
schema 合法
handoff 正确
evidence 可追溯
review 可闭环
compound 可沉淀
```

## 19.3 Provider readiness tests

覆盖：

```text
provider missing
provider installed but no index
index stale
query exits 0 but returns invalid
query stderr has known error
fallback provider available
```

## 19.4 Cross-platform tests

覆盖：

```text
macOS bash/zsh
Linux bash
Windows PowerShell 5.1
Windows PowerShell 7+
路径带空格
路径带中文
非 Git repo
单 repo 多 module
```

## 19.5 Review quality tests

构造 PR fixture：

```text
缺少 CHANGELOG
缺少测试
需求偏离
影响面遗漏
跨平台路径 bug
provider readiness 假阳性
安全风险
文档代码不一致
```

---

# 20. 关键维护者决策

## 决策 1：不做 another coding agent

原因：

```text
Claude Code / Codex / Copilot / Gemini CLI 会持续变强。
spec-first 应站在它们之上，做工程控制层。
```

## 决策 2：不做重状态机

原因：

```text
spec-first 的优势是 light contract。
重状态机会降低灵活性，并制造流程债。
```

## 决策 3：先 repo-local，再 workspace，再 team

路径：

```text
repo-local artifacts
→ workspace advisory
→ team governance
→ future platform optional
```

## 决策 4：review 是核心壁垒

原因：

```text
AI 写代码会越来越便宜。
证明代码是对的会越来越值钱。
```

## 决策 5：compound 是长期复利

原因：

```text
普通 agent 的产物是代码。
spec-first 的产物是代码 + 证据 + 决策 + 复盘 + 可复用知识。
```

---

# 21. 终版路线图总表

| 阶段 | 版本                     | 当前问题           | 未来问题                         | 核心结果                  |
| -- | ---------------------- | -------------- | ---------------------------- | --------------------- |
| 0  | Product Truth          | 用户不理解定位        | 避免被误解成 prompt collection     | Harness 叙事统一          |
| 1  | Harness Contract       | skill 边界不稳     | 为 skills/plugins 标准化做准备      | stage contract        |
| 2  | Evidence & Graph       | plan/review 靠猜 | 为多 agent 共享 evidence 做准备     | evidence packet       |
| 3  | Context + Tool Routing | 上下文混乱          | 为 MCP/tool 爆炸做治理             | context bundle        |
| 4  | Review Closure         | AI 写完无法证明对     | 为 agent self-review 做准备      | finding→fix→re-review |
| 5  | Long-running Sessions  | 会话中断不可恢复       | 为 overnight agents 做准备       | run state             |
| 6  | Knowledge Compound     | 经验不沉淀          | 为 repo-local memory 做准备      | compound delta        |
| 7  | Capability Pack        | 能力扩展无边界        | 为 skill/plugin ecosystem 做准备 | capability pack       |
| 8  | Team Governance        | 团队无法度量效果       | 为企业 AI 工程治理做准备               | metrics summary       |
| 9  | v1.0                   | 完整闭环稳定         | 可正式推广                        | Stable Harness        |

---

# 22. 最终结论

spec-first 的终版规划不是“继续做一个更强的 spec 工具”，而是：

```text
面向 AI coding 未来形态，
构建一套 repo-local、evidence-governed、review-closed、knowledge-compounding 的 Harness。
```

最终路线：

```text
0.x：
从 spec-driven workflow 走向 Harness contract。

0.x：
从 prompt/skill collection 走向 evidence-governed workflow。

0.x：
从单次 AI 任务走向 review-closed engineering loop。

0.x：
从聊天经验总结走向 repo-local knowledge compound。

1.0：
Stable AI Coding Harness。

1.x：
multi-agent / long-running / capability pack / team governance。
```

最重要的一句话：

```text
spec-first 不仅要解决今天 AI coding 混乱的问题，
还要提前解决未来 agents 太多、tools 太多、context 太大、review 太难、长期任务不可控的问题。
```

最终定位保持坚定：

```text
spec-first = AI coding 时代的 repo-local engineering Harness
```

它的价值不是让 AI 多写代码，而是让 AI coding：

```text
有需求锚点
有上下文路由
有证据链
有任务交接
有质量审查
有修复闭环
有知识复用
有团队治理
```

这就是 spec-first 下一阶段的终版规划。

[1]: https://github.github.com/spec-kit/?utm_source=chatgpt.com "Spec Kit Documentation"
[2]: https://developers.openai.com/codex/skills?utm_source=chatgpt.com "Agent Skills – Codex"
[3]: https://www.anthropic.com/engineering/code-execution-with-mcp?utm_source=chatgpt.com "Code execution with MCP: building more efficient AI agents"
[4]: https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents?utm_source=chatgpt.com "Effective harnesses for long-running agents"
[5]: https://github.blog/ai-and-ml/github-copilot/whats-new-with-github-copilot-coding-agent/?utm_source=chatgpt.com "What's new with GitHub Copilot coding agent"
