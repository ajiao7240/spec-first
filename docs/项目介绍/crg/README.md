# CRG 历史方案索引

> Lifecycle: historical-input / external-reference. 本目录保留历史 CRG/CE/ECC 方案、迁移或对比材料；其中 `src/crg`、`spec-first crg`、`graph.db`、`better-sqlite3`、`.claude-plugin`、命令数量和文件数量等旧口径可能已过期。当前 source of truth 以 `docs/archive-index.md`、`docs/README.md`、根目录 README、`docs/05-用户手册/`、`docs/contracts/`、`skills/`、`src/cli/`、`CHANGELOG.md`、`spec-mcp-setup` 和 `spec-graph-bootstrap` 为准。

本目录用于保留 2026-04 期间围绕 CRG、Stage-0、cross-repo 和 graph-bootstrap 的历史方案输入。阅读这些文档时：

- 只把它们作为演化背景、迁移依据或被拒绝方案的证据。
- 不把其中的 `src/crg`、`graph.db`、SQLite 或旧 CLI 子命令描述当成当前实现。
- 当前 graph/provider readiness 以 `spec-mcp-setup`、`spec-graph-bootstrap`、`.spec-first/graph/*`、`.spec-first/providers/*`、`.spec-first/impact/*` 和 `docs/contracts/graph-provider-consumption.md` 为准。
