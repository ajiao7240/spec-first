# 未完成技术方案清单与建议执行顺序

- 日期：2026-06-15
- 范围：全量扫描 `docs/plans/*.md`（182 份）、`docs/06-待办事项/*.md`（11 份）、`docs/03-实施方案/*.md`（13 份）的 frontmatter `status` 与正文进度标记
- 性质：跨 plan **只读协调视图**，非 plan 自身修订；各方案状态仍以其文件自身为准
- 取代：`docs/06-待办事项/2026-05-25-未完成-plan-执行顺序.md`（已严重过时，见下文）

## 关键更正：旧索引已失效

旧索引 `2026-05-25-未完成-plan-执行顺序.md` 点名的 6 份 active plan，现已全部完结或被取代：

| 旧索引点名 | 当前真实 status |
|---|---|
| `2026-05-25-001` GitNexus-only | `completed` |
| `2026-05-14-002` multi-actor worktree | `completed` |
| `2026-05-08-001` source-code deferred tracker | `closed` |
| `2026-05-22-003` CRG impact reviewer | `superseded` |
| `2026-05-20-001` spec-work skill quality fix | `superseded` |
| `2026-05-18-001` CRG primary, GitNexus optional | `superseded` |

**`docs/plans/` 目录 182 份全部为 `completed`(134) / `superseded`(47) / `closed`(1)，无一份 `active`。** 真正未完成的工作不在 `docs/plans/`，而集中在 `06-待办事项/` 与 `03-实施方案/`。

## 总体分布

| 位置 | 总数 | completed | superseded | closed | active / 草案 / 无状态历史文档 |
|------|------|-----------|------------|--------|------|
| `docs/plans/` | 182 | 134 | 47 | 1 | 0 |
| `docs/06-待办事项/` | 11 | 1 | 0 | 0 | 1 active + 1 active backlog + 8 历史/背景文档 |
| `docs/03-实施方案/` | 13 | — | — | — | 1 草案 + 12 规范/历史文档 |

## 未完成技术方案清单

| # | 方案 | 位置 | 状态 | 性质 |
|---|------|------|------|------|
| 1 | workflow harness evidence loop（`2026-05-14-001`） | 06-待办事项 | `active` | 唯一明确 active 的多版本路线图，5 个剩余执行 slice |
| 2 | 用户体感提升路线图 / P-friction（`2026-05-28`） | 06-待办事项 | `active backlog` | 路线图骨架，含 4 个未升格 candidate；前置已满足 |
| 3 | 场景自适应实施方案（`2026-05-28`） | 03-实施方案 | 草案，"尚未进入 spec-plan" | 技术方案草案，未升格为 plan，早于多轮硬切演进 |
| O1 | O-compaction：run.json 对接 host compaction 信号 | 06-待办事项（路线图内） | 观察项 | 未达候选成熟度，不排期 |
| O2 | O-workflow-metric：workflow 级结果质量度量 | 06-待办事项（路线图内） | 观察项 | 未达候选成熟度，不排期 |

> `06-待办事项/` 与 `03-实施方案/` 其余无 frontmatter 文档已逐一核实为**历史完成记录或背景结论**（如 `03-非bootstrap-skill控制面路径迁移` 标 ✅ 已完成、`11.仓库级控制面同步策略` 是产品边界结论、`2026-04-16 双宿主整改清单/backlog` 为历史审计），不属于未完成方案。

## 各项详情

### 1. `2026-05-14-001` workflow harness evidence loop（active）

按 plan 自身「建议剩余执行顺序」节，已放弃原始 U1-U9 线性顺序，剩余 5 个 slice：

1. **Schema budget refresh** — 决定 `evidence-packet` / `stage-contract` / `artifact-header` 是否新增。`docs/contracts/evidence-packet.md` 仍 MISSING，但 `artifact-summary`、`spec-work-run-artifact`、`verification-run-summary` 底座已存在。
2. **Core workflow adoption** — plan/work/review/doc-review/compound 对现有 summary/context/finding/run-artifact 的读取顺序 + focused tests。
3. **Review closure artifacts** — `fix-plan` / `re-review` / `compound-candidates` 的 producer、canonical path、consumer、fallback。
4. **Context budget & dispatch policy** — 在已有 `context-bundle` helper 上补 tool budget、provider risk、researcher budget、scale-aware dispatch tests。
5. **Release/evaluation guard** — runtime capability catalog、README/docs、package delivery、dual-host governance、source/runtime parity 的 deterministic drift guard。

**注意：** 近期 commit（`feat(verification): extend closeout evidence across workflows`、`verification-run-summary.v1` 三 workflow 共享）已部分蚕食 U7/U8 范围，该 plan 实际剩余面比 frontmatter 显示的小。**执行前必须先做一次 reconcile**，核销已落地项，避免重复开发。

### 2. `2026-05-28` 用户体感提升路线图 / P-friction（active backlog）

前置依赖 `2026-05-28-004-feat-spec-work-run-evidence-and-invariant-lens-plan.md` 已 `completed`，`.spec-first/workflows/**/run.json` 实际样本 **25 条 ≥ 10**——**P-friction 启动条件已全部满足**。

这是路线图明确指定的「第一个必然起草目标」。4 个 candidate：

- **P-friction**：spec-first 用户摩擦实证审计（启动条件已满足，第一目标）
- **P-task-pack-default**：task-pack 编译成本降到 medium/large 默认路径
- **P-graph-primary**：graph evidence advisory → primary 升级（独立轨道，可能需 GitNexus 上游协作）
- **P-review-inline**：small/medium 任务 closeout 内联轻量 review

后 3 个 candidate 的启动与优先级由 P-friction 实证 finding 决定，**路线图不预设**。

### 3. `2026-05-28` 场景自适应实施方案（草案）

正文明确标注「实施方案草案，尚未进入 spec-plan」。该草案早于 GitNexus-only 硬切等多轮后续演进，**有过时风险**。需先做架构一致性 reconcile，再决定升格为 plan 或归档。

## 建议执行顺序

```text
Phase 0（治理收口，几分钟）
  └─ 作废/标记 docs/06-待办事项/2026-05-25-未完成-plan-执行顺序.md
     （其点名的 6 份 plan 已全部完结，持续误导后续 brainstorm/plan）

Phase 1（立即可启动，实证驱动）
  └─ ① P-friction 升格为正式 plan —— 启动条件已满足（run.json 25 条 ≥ 10）
        路线图钦定第一目标；其 finding 决定后续 3 个 candidate 优先级
        先做它能避免 #2/#3 凭直觉排序

Phase 2（与 P-friction 解耦，可在空隙穿插推进）
  └─ ② 2026-05-14-001 剩余 slice —— 执行前先 reconcile
        近期 verification closeout 变更已覆盖部分 U7/U8，先核销已落地项，
        再按 Schema budget refresh → Core workflow adoption 顺序推进最小切片

Phase 3（等 P-friction finding 后再决策）
  └─ ③ P-task-pack-default / P-graph-primary（独立轨道，二选一或并行）
  └─ ④ P-review-inline（依赖 P-task-pack-default 落地 + review 摩擦被证实）
  └─ ⑤ 场景自适应实施方案 —— 先 reconcile 架构一致性，再决定升格/归档

不排期
  └─ O-compaction / O-workflow-metric：待 P-friction 积累样本后判断是否升格
```

**核心判断：** 真正的「开发型」未完成只有 ①（P-friction 升格）和 ②（harness loop 剩余 slice）。① 依赖已满足、ROI 最高、且能为后续排序提供证据，应排第一；② 可解耦穿插。③④⑤ 应等实证 finding 或 reconcile 后再启动，避免过度承诺。

## 依赖与边界

```text
2026-05-28-004 (completed) ──> P-friction (条件已满足) ──┬──> P-task-pack-default
                                                        ├──> P-graph-primary (独立轨道)
                                                        └──> P-review-inline (依赖 task-pack-default)

2026-05-14-001 ──── 独立，先 reconcile 已落地项再推进
场景自适应实施方案 ── 先做架构一致性 reconcile 再决定升格/归档
```

- 本文档是**只读协调视图**，不修改任何 plan/方案的 frontmatter 或内容；推进时仍以各文件自身为准
- 任何方案状态变化（升格 / completed / superseded / 归档）必须同步 `CHANGELOG.md`
- `2026-05-28` 场景自适应草案与 `2026-05-14-001` harness loop 都早于近期多轮演进，启动前的 reconcile 是硬前置，不可跳过

## 验证

本文档结论由以下确定性命令产出，LLM 仅做语义归类与排序判断：

```bash
# 全量 plan frontmatter status 分布
for f in docs/plans/*.md; do awk '/^---$/{c++;next} c==1&&/^status:/{sub(/^status:[ ]*/,"");print;exit} c>=2{exit}' "$f"; done | sort | uniq -c | sort -rn

# 06-待办事项 / 03-实施方案 逐文件 status
for f in docs/06-待办事项/*.md docs/03-实施方案/*.md; do awk '/^---$/{c++;next} c==1&&/^status:/{sub(/^status:[ ]*/,"");print FILENAME;exit} c>=2{exit}' "$f"; done

# P-friction 启动条件：run.json 真实样本数（需 ≥ 10）
find .spec-first/workflows -name run.json | wc -l   # → 25

# 前置 plan 2026-05-28-004 状态
awk '/^---$/{c++;next} c==1&&/^status:/{print;exit}' docs/plans/2026-05-28-004-*.md   # → completed
```
