# 未完成 Plan 清单与建议执行顺序

- 日期：2026-05-25
- 范围：扫描 `docs/plans/*.md` 共 127 份，过滤出未完成方案
- 性质：跨 plan 协调视图，**非 plan 自身修订**；plan 状态以各文件 frontmatter `status` 为准

## 总体分布

| 状态 | 数量 |
|------|------|
| `completed` | 81 |
| `superseded` | 14 |
| 旧设计文档（无 frontmatter status） | 26 |
| `active` | 5 |
| `backlog` | 1 |

未完成范围聚焦在 5 份 active + 1 份 backlog，共 **6 份**。

## 未完成 Plan 清单

| # | Plan | 状态 | 摘要 |
|---|------|------|------|
| 1 | `2026-05-25-001 GitNexus-only graph provider hard cut` | active（当前分支 `leo-2026-05-25-rm-crg`） | 删除 CRG，GitNexus 单 provider；含 U1–U9 + D1–D10 共 19 个治理/实施单元 |
| 2 | `2026-05-22-003 CRG impact reviewer agent` | active | 给 spec-code-review 新增 `spec-crg-impact-reviewer`；U1–U3 |
| 3 | `2026-05-20-001 spec-work skill quality fix` | active（两阶段） | fix-1 ship-now（编号 6→7）；fix-2 等 GitNexus 演进合并后做 |
| 4 | `2026-05-18-001 CRG primary, GitNexus optional` | active（**实质已被 5-25 反向替代**） | 方向与 5-25-001 相反 |
| 5 | `2026-05-14-001 workflow harness evidence loop` | active（三版本路线图） | v1.9 / v2.0 / v2.1，U1–U7，独立中长期 |
| 6 | `2026-05-08-001 source-code deferred tracker` | backlog | 持续维护型跟踪器，不阻塞 |

## 关键依赖图

```text
5-25-001 (GitNexus-only)  ──┬── 阻塞 ──> 5-22-003 (re-scope)
                            ├── 阻塞 ──> 5-20-001 fix-2
                            └── 反向替代 ──> 5-18-001 (mark superseded)

5-20-001 fix-1   ──── 可与 5-25 同分支，独立小动作
5-14-001         ──── 独立，可任何空隙推进
5-08-001         ──── 持续维护
```

## 建议执行顺序

### Phase A：当前分支硬切收尾（必须先做）

1. **5-25-001 GitNexus-only hard cut**（U1→U9 全量落地）
   - 当前分支最近 commit 仍以文档/skills/tests/plan 修订为主，**核心代码硬切（U2 setup registry、U3 graph-bootstrap execution、U4 non-git folder target、U5 review-pre-facts 切换、U7 tests/CI gate、U8 upgrade projection）尚未完整落地**
   - 这是阻塞其他 plan 的根
   - 验收门：`npm run test:unit` + `npm run test:smoke` + `npm run test:integration` + U9 一次性 runtime mirror audit
   - U1 GitNexus impact adapter 必须先于 U2/U3 删除 CRG（plan Risks 已标注 review downgrade risk）

2. **5-18-001 标记 superseded**（治理动作，1 个 commit，几分钟）
   - 5-25-001 已反向决策；5-18-001 frontmatter `status` 改 `superseded`，body 顶部加 supersede 指针，避免后续误读

### Phase B：5-25 合并后立刻做（小步快跑）

3. **5-20-001 fix-1**（一处编号 6→7 + 双宿主 runtime regenerate）
   - 与 5-25 是同向修复，**可在 5-25 合并前同分支顺带做**；plan 风险 8 已确认与现有 152 行 GitNexus 修改不冲突

4. **5-22-003 re-scope**（CRG impact reviewer 决定去留）
   - 5-25 落地后 CRG 已不在 active provider，本 plan 二选一：
     - A. 改名 + 改源：重命名为 `spec-graph-impact-reviewer`，evidence 来源切到 GitNexus canonical impact adapter（U1 of 5-25-001）
     - B. 撤销 plan：若 GitNexus impact adapter 已经覆盖等价能力，关闭此 plan
   - 先决策 A/B 再投入 U1–U3 实施

5. **5-20-001 fix-2**（U8 Execution Boundary H3 子段 + source_note 改写）
   - 启动条件由 plan 自定义为「GitNexus 演进合并到 master 后」

### Phase C：独立中长期路线图

6. **5-14-001 workflow harness evidence loop** 按版本切片推进：
   - v1.9：U1（紧凑阶段合同）→ U2（证据包/Artifact Summary）→ U3（跨 review workflow finding）
   - v2.0：U4（context bundle helper）→ U5（证据源选择）→ U6（dispatch/research budget）
   - v2.1：U7（work 修复闭环 + review closure handoff）
   - 与 GitNexus-only 解耦，任何 phase 间隙都可推进 1 个 unit

### Phase D：持续维护

7. **5-08-001 source-code deferred tracker**
   - 每次 review/plan 收尾时按需追加/勾销条目，不在主线占资源

## 风险与边界

- 本文档是**只读协调视图**，不修改任何 plan frontmatter 或内容；推进各 plan 时仍以 plan 自身为准
- 5-18-001 与 5-25-001 的方向冲突需要在 Phase A.2 显式标记 superseded，否则会持续误导后续 brainstorm/plan 阶段
- 5-22-003 的命名仍含 `crg-impact-review`，后续 re-scope 时需要同步 spec_id、文件名、CHANGELOG，避免命名漂移
- 任何 plan 状态变化（superseded / completed）必须同步 `CHANGELOG.md`

## 验证

- `awk '/^---$/{c++; next} c==1 && /^status:/{print FILENAME, $0}' docs/plans/*.md`：核对 frontmatter status
- `grep -l 'status: active' docs/plans/*.md`：列出 active plan
- `git log --oneline main..HEAD -- docs/plans/2026-05-25-001-*`：核对 5-25 plan 实际进度
