# Spec-First Framework 目录结构

## 1. 目录视角

这个项目同时存在两种目录视角：

1. 仓库源码视角：`spec-first` npm 包本身包含的目录结构
2. 目标项目运行态视角：用户执行 `spec-first init --claude` 后，在目标项目里生成的 `.claude/` 目录

## 2. 仓库源码视角

```text
spec-first/
├── .claude-plugin/
│   └── plugin.json
├── bin/
│   └── spec-first.js
├── src/
│   └── cli/
│       ├── index.js
│       ├── commands/
│       │   ├── doctor.js
│       │   └── init.js
│       ├── plugin.js
│       ├── spec-commands.js
│       ├── skills.js
│       └── templates.js
├── skills/
│   ├── spec-brainstorm/
│   ├── spec-plan/
│   ├── spec-work/
│   ├── spec-review/
│   └── ...  # 全量随包发布的 skill 目录
├── agents/
│   ├── review/
│   ├── research/
│   ├── workflow/
│   └── ...  # 全量随包发布的 agent 目录
├── templates/
│   └── claude/commands/spec/
│       ├── brainstorm.md
│       ├── plan.md
│       ├── work.md
│       ├── review.md
│       └── compound.md
├── docs/
├── tests/
│   ├── smoke/
│   │   ├── cli.sh
│   │   └── install-local.sh
│   └── integration/
│       └── e2e.sh
├── package.json
└── README.md
```

## 3. 目标项目运行态视角

`spec-first init --claude` 会在目标项目里生成：

```text
your-project/
└── .claude/
    ├── commands/
    │   └── spec/
    │       ├── brainstorm.md
    │       ├── plan.md
    │       ├── work.md
    │       ├── review.md
    │       └── compound.md
    ├── skills/
    │   ├── spec-brainstorm/
    │   ├── spec-plan/
    │   ├── spec-work/
    │   ├── spec-review/
    │   └── ...  # 与 npm 包内 skills/ 同步
    ├── agents/
    │   ├── review/
    │   ├── research/
    │   ├── workflow/
    │   └── ...  # 与 npm 包内 agents/ 同步
    └── ...
```

## 4. 说明

- `templates/claude/commands/spec/` 是 npm 包内部模板来源，不是目标项目运行态目录
- `.claude/commands/spec/` 是用户真正执行 `/spec:*` 时的命令来源，这层只负责稳定入口
- `.claude/skills/` 是 `spec-first init --claude` 同步下发的 skill 运行态目录，用于保留发布后可引用的完整 skill 资产
- `.claude/agents/` 是 `spec-first init --claude` 同步下发的 agent 运行态目录，用于给 skills 调度子代理
- `.claude-plugin/plugin.json` 是发布包内的统一资产清单，`init` / `doctor` 会基于它发现 commands、skills、agents
- 当前流程的稳定产物以 `docs/brainstorms/`、`docs/plans/`、`docs/solutions/` 为主，不要求目标项目存在 `.claude/tasks/`
- `skills/` 和 `agents/` 现在属于 npm 包发布产物；`scripts/` 仍属于仓库级开发与历史资产，不进入发布包
