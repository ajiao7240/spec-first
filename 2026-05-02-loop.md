# 2026-05-02 spec-app-consistency-audit 执行记录

## 任务快照

记录时间：2026-05-02 05:16:01 CST

当前顶层目标：

1. 按 `$spec-work` 执行 `docs/02-架构设计/spec-app-consistency-audit/升级技术方案_完整协议参考.md`。
2. 基于该方案，对现有 `skills/spec-app-consistency-audit` 做详细改造。
3. 改造后执行 `$spec-code-review` 风格代码审查。
4. 审查完成后，对发现的问题自主决策、逐项修复，不等待用户确认。
5. 每个问题都在本文件记录：问题描述、具体修复细节、修复结论、修复效果。
6. 继续围绕项目整体，对 `spec-app-consistency-audit` 做 50 轮小步代码质量审查与修订。
7. 全程严格遵守 `docs/10-prompt/结构化项目角色契约.md`。

## 强制角色与判断基线

- 当前角色：Spec-First Evolution Architect。
- 核心原则：`Light contract`、`Explicit boundaries`、`Scripts prepare, LLM decides`。
- 核心链路：`Codebase -> Graph -> Spec -> Plan -> Tasks -> Code -> Review -> Knowledge`。
- 关键边界：修改 source-of-truth，不手改 `.claude/`、`.codex/`、`.agents/skills/` generated runtime mirror。
- 脚本职责：确定性 facts、schema 校验、hash、artifact 写入、reason_code。
- LLM 职责：影响面解释、专家选择、风险判断、证据语义充分性、下一步建议。
- 用户已授权：遇到卡点、权限、取舍、修复路线时由 Codex 自主决策推进，不再中断确认。

## 当前执行边界

本轮优先落地方案中的 v0.1a contract spine，并为 v0.1b 保留最小 schema / prompt / planner 骨架：

- mode contract：`default` / `mode:headless` / `mode:report-only` / `from:code-review` / `depth:deep`。
- run-scoped artifacts：`.spec-first/app-audit/runs/<run-id>/`。
- report-only no-write：不写 run artifacts、不 materialize Figma、不写 preview。
- headless envelope：供 `spec-code-review` 或父 workflow 消费。
- metadata / manifest：`metadata.json`、`artifact-manifest.json`、freshness、diff_hash、worktree_fingerprint。
- impact facts：`impact-facts.json` 只包含候选 facts/signals，不确认 issue。
- audit context：`app-audit-context.json` 合并候选 facts 和 degraded modes。
- issue hardening：`claim_family`、`claim_type`、`affected_surface`、`validation_status`、`code_review_handoff`。
- evidence gate：拒绝无证据、rule-pack-only confirmed、未确认行业 confirmed。
- workflow handoff：v0.1 只写 summary/headless follow-up，不落独立 `workflow-handoff-suggestions.json`。

## 当前已完成上下文

- 已读取角色契约。
- 已读取 `using-spec-first`、`spec-work`、`spec-code-review` workflow source。
- 已读取完整协议参考文档关键章节。
- 当前分支：`leo-2026-05-02-app`。
- 初始工作区：干净。
- developer profile：`.codex/spec-first/.developer` 中作者为 `leokuang`。
- GitNexus 影响分析：
  - `parseCommonArgs`：MEDIUM，直接影响 13 个 extractor / merge CLI。
  - `applyEvidenceGate`：LOW。
  - `buildAuditReport`：LOW。
  - `validateAuditIssue`：LOW。
  - `buildAuditContext`：LOW。
  - `runPreflight`：LOW。

## 当前已开始的改造

已开始修改以下 source 文件：

- `skills/spec-app-consistency-audit/scripts/lib/audit-utils.js`
  - 扩展 canonical token 和 mode parsing。
  - 支持 `mode:*`、`base:*`、`source:*`、`prd:*`、`figma-context:*`、`figma-ref:*`、`industry:*`、`tech-plan:*`、`task-doc:*`、`depth:*`、`from:*`、`expected-input:*`。
  - 加入 report-only 禁止 `--output` 的确定性保护。

- `skills/spec-app-consistency-audit/scripts/preflight.js`
  - 切换到共享 `parseCommonArgs`，避免 preflight 与其他 CLI 参数语义漂移。

- 新增 `skills/spec-app-consistency-audit/scripts/build-impact-facts.js`
  - 生成 source-only impact facts。
  - 输出 changed files、candidate signals、interaction_surface_changed minimal signals、coverage_capabilities、input_expectations、audit_verdict_scope。
  - `mode:headless` 缺少 `base:<ref>` 时失败。

- 新增 `skills/spec-app-consistency-audit/scripts/build-run-metadata.js`
  - 生成 run-scoped metadata。
  - 记录 branch、head_sha、diff_hash、worktree_fingerprint、coverage_capabilities、input_expectations、summary_path、issues_path。

- 新增 `skills/spec-app-consistency-audit/scripts/build-artifact-manifest.js`
  - 生成 run 内 artifact manifest。
  - 记录 artifact path、schema_version、artifact_id、producer、consumers、sha256、freshness、data_sensitivity、degraded reason codes。

- 新增 `skills/spec-app-consistency-audit/scripts/render-headless-envelope.js`
  - 根据 metadata/report 渲染 headless envelope。
  - 保留 terminal signal：`App consistency audit complete`。

## 后续必须继续完成

1. 补 `audit-plan` 最小 schema 与 builder/prompt 边界，确保 LLM planner 是语义产物而非脚本规则。
2. 加固 `merge-contracts.js`：
   - issue normalize / lifecycle。
   - deterministic evidence gate reason_code。
   - claim_family conclusion cap。
   - industry confirmed 边界。
   - code_review_handoff 映射。
3. 加固 `validate-artifacts.js`：
   - 新增 strict issue mode。
   - 新 schema 校验：impact-facts、metadata、manifest、issue、audit-plan。
4. 更新 `SKILL.md`：
   - mode/headless/report-only。
   - run-scoped artifacts。
   - report-only no-write。
   - v0.1a/v0.1b 分期。
   - source/runtime、security/redaction、handoff 边界。
5. 更新 Claude command template argument hint。
6. 补单测：
   - canonical token parsing。
   - headless 缺 base failure。
   - report-only no-write。
   - run-scoped CLI e2e。
   - strict issue schema。
   - rule-pack-only / unconfirmed industry rejection。
   - headless envelope。
7. 更新 `CHANGELOG.md`。
8. 运行最窄验证：相关 unit tests、`npm run typecheck` 或具体 `node --check`。
9. 执行 code review 风格审查并把发现、修复、效果追加到本文件。
10. 执行 50 轮小步审查与修订，每轮追加记录。

## 执行进展记录

### 2026-05-02 05:25 Contract Spine 初步落地

问题描述：

- 现有 `spec-app-consistency-audit` 仍偏静态 MVP 文案和扁平 artifact 流程，缺少升级方案要求的 mode/headless/report-only/run-scoped/impact-facts/strict issue/handoff 契约。
- 共享参数解析器没有 canonical token，preflight 与 extractor CLI 容易参数语义漂移。
- evidence gate 只检查项目证据和 rule-pack-only confirmed，缺少 claim_family、未确认行业、review_lifecycle、code_review_handoff 等 v0.1a/v0.1b 边界。

具体修复细节：

- 扩展 `scripts/lib/audit-utils.js`：
  - 新增 canonical token 解析：`mode:*`、`base:*`、`source:*`、`prd:*`、`figma-context:*`、`figma-ref:*`、`industry:*`、`tech-plan:*`、`task-doc:*`、`depth:*`、`from:*`、`expected-input:*`。
  - 加入 `mode:report-only` 禁止 `--output` 的确定性保护。
  - 新增 run id / run dir helper。
- 更新 `scripts/preflight.js` 使用共享参数解析器。
- 新增 contract spine scripts：
  - `build-impact-facts.js`
  - `build-run-metadata.js`
  - `build-artifact-manifest.js`
  - `render-headless-envelope.js`
- 更新 `scripts/build-audit-context.js`，让 `app-audit-context` 成为带 metadata 的 run artifact。
- 加固 `scripts/merge-contracts.js`：
  - issue normalize。
  - deterministic evidence gate lifecycle。
  - claim_family evidence requirement。
  - rule-pack-only confirmed rejection。
  - unconfirmed industry confirmed rejection。
  - code_review_handoff 映射。
  - `--issues-artifact` 输出 strict issues artifact。
- 加固 `scripts/validate-artifacts.js`：
  - strict issue mode。
  - issues artifact validation。
  - metadata / manifest / impact-facts / audit-plan / app-audit-context validation。
- 新增 schemas：
  - `impact-facts.schema.json`
  - `audit-plan.schema.json`
  - `issue.schema.json`
  - `issues.schema.json`
  - `metadata.schema.json`
  - `artifact-manifest.schema.json`
  - `app-audit-context.schema.json`
- 新增 `prompts/audit-planner.md`，并更新 orchestrator / evidence-auditor / report-writer prompt 边界。
- 更新 `SKILL.md` 和 Claude command argument hint，写明 mode contract、run-scoped artifacts、report-only no-write、headless envelope、v0.1a/v0.1b 分期、安全与 handoff 边界。
- 更新测试：
  - evidence gate。
  - report generation。
  - artifact validation。
  - entry contract。
  - CLI e2e。
  - prompt contracts。

修复结论：

- Contract spine 已可通过聚焦测试运行。
- `mode:headless`、run-scoped path、impact facts、strict issue、headless envelope、manifest 和 validation contract 已有最小可执行覆盖。
- LLM Audit Planner 仍保持 prompt/schema 边界，没有用脚本替代语义专家选择。

修复效果：

- 已通过：

```text
npx jest tests/unit/spec-app-consistency-audit-evidence.test.js tests/unit/spec-app-consistency-audit-report.test.js tests/unit/spec-app-consistency-audit-validate.test.js tests/unit/spec-app-consistency-audit-entry.test.js tests/unit/spec-app-consistency-audit-cli-e2e.test.js tests/unit/spec-app-consistency-audit-prompts.test.js --runInBand

Test Suites: 6 passed, 6 total
Tests: 26 passed, 26 total
```

残留：

- 还需要跑完整 app-audit 相关测试集。
- 还需要做 code-review 风格审查与 50 轮小步审查记录。
- 还需要根据最终 diff 更新更完整的 CHANGELOG 记录。

### 2026-05-02 05:39 断点恢复快照

问题描述：

- 本轮是跨多文件、跨 workflow contract 的长任务，执行中已经进入 code-review 风格审查阶段。
- 用户明确要求任何卡点、权限、路线取舍都由 Codex 自主决策推进，不等待确认。
- 需要把当前任务、强制原则、已知审查发现、后续修复队列和验证要求写入临时文档，避免会话压缩或中途恢复时丢失上下文。

当前任务要求：

1. 按 `$spec-work` 执行 `docs/02-架构设计/spec-app-consistency-audit/升级技术方案_完整协议参考.md`。
2. 根据方案继续完成 `skills/spec-app-consistency-audit` 的详细改造。
3. 按 `$spec-code-review` 做代码审查，审查发现由 Codex 自主逐项修复。
4. 持续围绕项目整体对 `spec-app-consistency-audit` 做 50 轮小步代码质量审查与修订。
5. 每个问题都记录问题描述、具体修复细节、修复结论、修复效果。
6. 严格遵守 `docs/10-prompt/结构化项目角色契约.md`。

强制执行边界：

- 角色：Spec-First Evolution Architect。
- 原则：`Spec > Code`、`Systems > Prompts`、`Light contract`、`Explicit boundaries`、`Scripts prepare, LLM decides`。
- 主链路：`Codebase -> Graph -> Spec -> Plan -> Tasks -> Code -> Review -> Knowledge`。
- source-first：优先修改 `skills/`、`templates/`、`tests/`、`docs/`、`CHANGELOG.md` 等 source-of-truth。
- 禁止手改 generated runtime：`.claude/`、`.codex/`、`.agents/skills/`。
- 所有 source 变更都要按当前仓库格式更新 `CHANGELOG.md`，作者为 `leokuang`。
- 已知无关工作区变更：根目录旧 loop 文档删除属于既有未提交状态，本任务不恢复、不回滚。

当前已知 reviewer findings 修复队列：

- P1：`merge-contracts.js` 的 project evidence gate 可被带 `rule_pack` 字段的 `evidence.code` 绕过。后续需要先排除 rule-pack evidence，再判断 project evidence，并要求至少有 `file` / `path` / `artifact_id` / `node_id` / `route` / `event` / `key` 等可追溯字段。
- P1：industry confirmed gate 不能信任 issue 自带 `industry_confirmed` / `confirmed_industry`，只能信任 deterministic upstream artifact 或受校验的 profile 输入；无 confirmed profile 时，`industry_compliance` confirmed issue 必须降级或拒绝。
- P1：`build-run-metadata.js` 的 `diff_hash` 与 `base:<ref>` headless 审查范围不一致；需要复用 base diff scope，或显式拆分 review diff 与 working-tree diff。
- P1：`base:<ref>` 未校验即可进入 `git diff` / `git merge-base`，存在 Git option 注入与 textconv 执行风险；需要拒绝危险 ref，使用 `git rev-parse --verify --end-of-options <base>^{commit}` 解析安全 commit SHA，并对 diff 使用 `--no-textconv`。
- P1：headless envelope、report 和 issues artifact 会输出不可信 issue 文本；需要统一 artifact text redaction / truncation，并在 headless 输出层再次 escape / truncate，覆盖 token URL、Authorization/Bearer、Cookie、secret key/value、长文本和换行控制字符。
- P1：`gateInputIssues()` 丢失上游 `issues.json` 中的 `rejected_issues`，导致 report/headless 无法保留 rejected transparency；需要合并保留 rejected findings。
- P1：`build-audit-context.js` 自行解析参数，绕过 `mode:report-only` no-write contract；需要改用共享 `parseCommonArgs()` 或同等 gate，并补 CLI 回归测试。
- P1：`build-impact-facts.js mode:headless` 缺少 `base:<ref>` 时只 stderr/throw，不输出 failed headless envelope；需要增加 failed envelope 渲染路径和 headless missing-base e2e。
- P2：`merge-contracts.js` / `SKILL.md` / `references/report-format.md` / tests 仍泄漏 legacy flat `.spec-first/app-audit/` 路径；需要统一改为 `.spec-first/app-audit/runs/<run-id>/...` 或明确只读迁移语义。
- P2：`preflight.js` 需要完整纳入 `figma-ref:<id-or-url>`，输出 reference-only degraded facts，并避免原文 URL 传播。
- P2：Figma reference 不应原样写入 preflight/source_inputs/context/report；需要只保留 `reference_provided`、`reference_kind`、`reference_hash` 和必要的脱敏 host/type。
- P2：PRD-derived contract 文本可能把 token URL/PII 固化到 artifact；需要复用 redaction helper，优先记录 `line_hash`、section、redacted summary 和长度限制。
- P2：`build-audit-context.js` 默认扫描 `.spec-first/app-audit` 可能混入旧 flat/sibling runs 或自包含 rerun 输出；需要收紧为显式 run dir，并排除 output/self/latest/manifest。
- P2：`validateIssuesArtifact()` 只校验 `issues[]`，不校验 `rejected_issues[]`；需要对 rejected issue 也执行 strict issue 校验，或改 schema。
- P2：`audit-report` validator 默认未执行 strict issue 字段校验；需要对最终 report 默认启用 strict issue validation，或明确 legacy/non-strict 只作迁移输入。
- P2：`SKILL.md` 列出 `evidence-gate-result.json`，但当前没有 schema/script/e2e 产出；v0.1a 应移除或明确真实来源是 `issues.json` / `audit-report.rejected_issues`。
- P2：`build-run-metadata.js` 默认 `host: codex` 会污染 Claude 入口 provenance；默认应改为 `unknown` 或由显式 env/adapter 注入。
- P2：`mode:report-only --output` 需要覆盖到 `build-audit-context.js`、metadata/manifest 等脚本的 no-write 行为。
- P3：`coverage_capabilities` / `input_expectations` 在 metadata 与 impact facts 间重复且有漂移；需要抽共享 helper 或补一致性测试。
- P3：manifest 需要更强内容断言，确保包含 metadata/preflight/impact/context/report/issues、producer、sha256、relative path，并排除 self。
- P3：headless envelope 测试过弱，需要覆盖 terminal signal、verdict、artifact paths、rejected findings、runtime verification、degraded modes。
- P3：空 manifest fallback 会泄漏绝对 run dir；fallback 也要用 `publicPath(...)`。

下一步执行顺序：

1. 修复安全与 contract P1：safe base ref、no textconv、headless failed envelope、report-only no-write、evidence gate 绕过、industry confirmed 信任边界、diff_hash scope、artifact text redaction。
2. 修复 P2/P3 contract drift：run-scoped writeback preview、Figma reference sanitizer、PRD redaction、context self/sibling exclusion、strict rejected/report validation、host provenance、manifest fallback。
3. 同步 `SKILL.md`、`references/report-format.md`、Claude command template、schemas 和 tests。
4. 追加每个问题的修复记录到本文件。
5. 运行 `node --check`、聚焦 Jest、必要时 `npm run typecheck`。
6. 运行 `gitnexus_detect_changes(scope: all)` 检查影响面。
7. 继续执行 50 轮小步审查与修订，不等待用户确认。

### 2026-05-02 05:58 Code Review Findings 第一批修复

问题描述：

- 多个 reviewer 指出 v0.1a contract spine 虽已落地，但存在 P1/P2 contract 破口：
  - `base:<ref>` 可被 Git option 注入，且 diff 未禁用 textconv。
  - `mode:headless` 缺 base 时只 stderr，不返回 headless failed envelope。
  - `build-audit-context.js` 绕过 `mode:report-only` no-write。
  - `merge-contracts.js` 可把伪装在 `evidence.code` bucket 中的 rule-pack 证据当成 project evidence。
  - industry confirmed gate 信任 issue 自带 `industry_confirmed` / `confirmed_industry`。
  - `metadata.diff_hash` 与 `base:<ref>` 审查范围不一致。
  - report/issues/headless 输出未统一脱敏不可信 issue 文本。
  - Figma reference 与 PRD-derived 文本可能把 URL token / raw text 写入 artifact。
  - `rejected_issues` 未被严格校验，且上游 rejected findings 会在 report 阶段丢失。
  - preview writeback、Figma input 示例和 report reference 仍有 legacy flat path。
  - metadata 与 impact facts 的 `coverage_capabilities` / `input_expectations` 存在重复逻辑和漂移。

具体修复细节：

- `scripts/lib/audit-utils.js`
  - 新增 `collectGitDiffFacts()`，统一 base diff 与 working-tree diff scope。
  - `base:<ref>` 先执行安全字符校验，再用 `git rev-parse --verify --end-of-options <ref>^{commit}` 解析成 commit SHA。
  - 所有 diff 命令增加 `--no-textconv` 和 pathspec `--`。
  - 新增 `redactForArtifactText()`，覆盖 token-bearing URL、Authorization/Bearer、Cookie、secret key/value、控制字符和长文本截断。
  - 新增 `buildFigmaReference()` / `sanitizeFigmaReference()`，Figma ref 只保留 kind、hash、host/type 等脱敏 metadata。
  - 新增共享 `buildAppAuditCoverageCapabilities()`、`buildAppAuditInputExpectations()`、`buildAppAuditVerdictScope()`，消除 metadata 与 impact facts 漂移。
- `scripts/build-impact-facts.js`
  - 改用共享 diff facts。
  - `mode:headless` scope 失败时输出 failed headless envelope，并保留 `App consistency audit complete` terminal signal。
- `scripts/build-run-metadata.js`
  - 改用同一 diff scope 计算 `diff_hash`，并记录 `resolved_base_sha` / `diff_scope_kind`。
  - 默认 `host` 从 `codex` 改为显式 env 或 `unknown`。
- `scripts/build-audit-context.js`
  - 改用 `parseCommonArgs()`，继承 `mode:report-only --output` 禁止写入。
  - 要求显式 `--artifacts-dir` 或 `run-dir:<path>`，不再默认扫描 legacy flat `.spec-first/app-audit`。
  - 排除自身输出、manifest、latest summary、`input/` 与 `writeback-preview/`，避免自包含和 raw input 被当 artifact。
  - 空 artifact fallback 使用 public path，不泄漏绝对路径。
- `scripts/build-artifact-manifest.js`
  - 排除 `input/` 和 `writeback-preview/`，避免把 raw Figma input 当 run artifact。
  - 空目录 fallback 改用 public path。
- `scripts/preflight.js`
  - `figma-ref` 纳入 reference-only 事实。
  - Figma reference 不再原样写入 `source_inputs` 或 `inputs.figma`。
- `scripts/extract-prd-contract.js`
  - PRD-derived rule/journey/page evidence 增加 redacted summary 与 `line_hash`，避免 token URL / PII 原文固化到 artifact。
- `scripts/merge-contracts.js`
  - `isProjectEvidence()` 先排除 rule-pack evidence，再要求 project evidence 具备可追溯字段。
  - `collectRulePackEvidence()` 覆盖伪装在 project bucket 中的 rule-pack evidence。
  - industry confirmed gate 不再信任 issue 自证 flags，只接受受校验的 `confirmedIndustry` ID。
  - report 和 issues artifact 在 normalize 阶段脱敏 title / impact / recommendation / evidence summary / runtime reason。
  - 上游 `rejected_issues` 在 report 阶段保留，并补齐 rejected lifecycle。
  - preview writeback path 改为 run-scoped；能从 `--run-dir`、`--output` 或 `run-id` 推导。
- `scripts/validate-artifacts.js`
  - audit report 默认启用 strict issue validation。
  - report / issues artifact 都校验 `rejected_issues[]`，错误 path 使用 `rejected_issues[index]`。
- `SKILL.md`、`references/report-format.md`、Claude command template
  - 移除未产出的 `evidence-gate-result.json`。
  - 增加 `audit-report.json` 与 `headless-envelope.txt`。
  - Figma input 和 writeback preview 示例统一到 `.spec-first/app-audit/runs/<run-id>/...`。
  - argument hint 增加 `figma-ref:<id-or-url>`。
- 测试新增/强化：
  - rule-pack disguised project evidence。
  - issue 自证 industry confirmed。
  - unsafe `base:--output=...` 不写文件。
  - headless missing base failed envelope。
  - `build-audit-context mode:report-only --output` no-write。
  - Figma ref URL token 脱敏。
  - `rejected_issues[]` strict validation。
  - metadata / impact facts coverage 与 input expectations 一致性。
  - manifest 内容、producer、sha256、self/raw input exclusion。
  - headless rejected findings 与 runtime verification 输出。

修复结论：

- P1 安全与 contract 破口已收敛为脚本可验证事实，不依赖 LLM 记忆。
- `base:<ref>`、headless failed envelope、report-only no-write、evidence gate、industry gate、artifact text redaction、Figma/PRD redaction、strict rejected validation、run-scoped preview path 已有回归覆盖。
- scripts 仍只负责确定性事实与结构校验；行业/profile 语义判断仍保留给 LLM 或上游 deterministic artifact。

修复效果：

- 已通过 `node --check`：
  - `audit-utils.js`
  - `build-impact-facts.js`
  - `build-run-metadata.js`
  - `build-audit-context.js`
  - `merge-contracts.js`
  - `validate-artifacts.js`
  - `preflight.js`
  - `render-headless-envelope.js`
- 已通过完整 app-audit 单测：

```text
npx jest tests/unit/spec-app-consistency-audit-*.test.js --runInBand

Test Suites: 17 passed, 17 total
Tests: 66 passed, 66 total
```

残留：

- 需要继续执行 50 轮小步审查，重点看新增 helper 是否过宽、schema 是否需要同步 maxLength/redaction policy、headless failed envelope 是否需要更正式 schema、README/docs 是否需要用户可见同步。
- 需要在最终收口前运行 `npm run typecheck` 和 `gitnexus_detect_changes(scope: all)`。

### 2026-05-02 06:11 临时任务 Checkpoint

问题描述：

- 当前任务是跨 workflow、skill、schema、script、template、test 与 docs 的长任务，存在会话压缩或中断后丢失执行上下文的风险。
- 用户要求把当前任务和要求记录到临时文档，避免后续恢复时遗漏目标、边界、审查轮次和验证责任。

当前任务要求：

1. 严格以 Spec-First Evolution Architect 角色继续工作。
2. 始终遵守 `docs/10-prompt/结构化项目角色契约.md`。
3. 按 `$spec-work` 执行 `docs/02-架构设计/spec-app-consistency-audit/升级技术方案_完整协议参考.md`。
4. 根据升级方案继续完成 `skills/spec-app-consistency-audit` 的详细改造。
5. 按 `$spec-code-review` 风格执行代码审查，并对发现问题自主决策、逐项修复。
6. 持续围绕项目整体对 `spec-app-consistency-audit` 做 50 轮小步代码质量审查与修订。
7. 每个问题都要记录：问题描述、具体修复细节、修复结论、修复效果。
8. 遇到卡点、权限、路线取舍或需要确认的点，由 Codex 自主决策推进，不中断等待用户确认。

强制执行边界：

- 核心判断：`AI coding is not a prompt problem — it is a workflow problem`；`Spec > Code`；`Systems > Prompts`。
- 架构原则：`Light contract`、`Explicit boundaries`、`Scripts prepare, LLM decides`。
- 核心链路：`Codebase -> Graph -> Spec -> Plan -> Tasks -> Code -> Review -> Knowledge`。
- source-first：优先修改 `skills/`、`templates/`、`scripts/`、`tests/`、`docs/`、`CHANGELOG.md` 等 source-of-truth。
- 禁止手改 generated runtime：`.claude/`、`.codex/`、`.agents/skills/`。
- 所有 source 变更必须同步 `CHANGELOG.md`，作者使用 `.codex/spec-first/.developer` 中的 `leokuang`。
- 当前分支：`leo-2026-05-02-app`。
- 已知无关工作区变更：根目录旧 loop 文档删除属于既有未提交状态，本任务不恢复、不回滚。

当前已完成进度：

- 已完成 v0.1a/v0.1b contract spine 初步落地：
  - canonical args / modes。
  - run-scoped artifacts。
  - `mode:headless` 与 `mode:report-only`。
  - metadata / manifest / impact facts / app-audit context。
  - strict issue schema / rejected issues / handoff。
  - audit planner prompt。
  - validation 与聚焦测试覆盖。
- 已完成第一批 code-review findings 修复：
  - safe `base:<ref>` 解析与 diff hardening。
  - headless failed envelope。
  - report-only no-write。
  - evidence gate 与 industry confirmed gate 加固。
  - Figma ref、PRD text 与 issue text 脱敏。
  - strict rejected validation。
  - run-scoped preview 与 artifact path 收敛。
- 50 轮小步审查已推进到约第 19 轮：
  - schema strictness。
  - redaction policy。
  - metadata / manifest / context required fields。
  - preflight / impact-facts required fields。
  - report-only no-write tests。
  - 移除死代码 `SECRET_KEY_PATTERN`。

下一步恢复点：

1. 从第 20 轮开始继续执行 50 轮小步审查。
2. 优先审查：
   - `extract-figma-contract.js` 是否与新脱敏策略一致。
   - `render-headless-envelope.js` 是否泄漏绝对路径。
   - report / summary artifact 是否泄漏 repo 外路径。
   - `validate-artifacts.js` strict validation 是否覆盖新增 schema 约束。
   - `build-run-metadata.js` absolute `runDir` 输出是否保持 public path。
   - `build-audit-context.js` 对 invalid artifact 的失败语义是否符合 workflow。
   - README / docs 是否需要补充用户可见 mode contract。
   - schema tests 是否真正断言新增必填字段。
   - generated runtime 是否保持未改动。
3. 每一批修复后继续追加本文件，保留问题描述、修复细节、结论和效果。
4. 任何新增 source 修改后补 `CHANGELOG.md`。
5. 最终收口前运行验证：
   - `npx jest tests/unit/spec-app-consistency-audit-*.test.js --runInBand`
   - `npm run typecheck`
   - `npm run lint:skill-entrypoints`
   - `git diff --check`
   - `mcp__gitnexus__.detect_changes(scope: all)`

修复结论：

- 当前任务与恢复要求已写入本临时执行文档。
- 后续可直接从“第 20 轮小步审查”继续，不需要重新确认任务目标。

修复效果：

- 降低长任务中断后的上下文丢失风险。
- 保留了用户授权的自主推进边界、source/runtime 边界、审查轮次状态和验证收口要求。

### 2026-05-02 06:23 第 20-50 轮小步审查修复

问题描述：

- 第 20-23 轮发现 `extract-figma-contract.js` 的 `internal` redaction 只按长度和少量敏感词判断，短 URL、Authorization、Cookie 等片段可能进入 `raw_label` / `raw_text`。
- 第 24-26 轮发现 extractor 修复后，外部或旧版 `figma-design-contract` artifact 仍可能携带未脱敏 raw label/text 并通过 validator。
- 第 27-29 轮发现 `render-headless-envelope.js` 对 metadata 中的 `run_dir`、`summary_path`、`issues_path` 只做 URL/secret 文本脱敏，不拦截绝对路径泄漏。
- 第 30-33 轮发现 strict issue validator 只检查 `title`、`impact`、`recommendation`，没有覆盖 `provenance[].summary`、`evidence.*[].summary`、`runtime_verification.reason` 和 path-like 字段。
- 第 34-36 轮发现 `merge-contracts.js` 的 issue normalize 未把外部 issue 中的 `affected_surface.file`、evidence/provenance file/path 统一转为 repo-relative 或 redacted public path，严格验证加固后可能导致 report 失败或泄漏本机路径。
- 第 37-39 轮发现 `build-artifact-manifest.js` 会把 run 目录内的 `latest-summary.json` 当作普通 run artifact；但 latest summary 是指针，不应成为事实源或 manifest evidence entry。
- 第 40-42 轮确认 schema/validator 同步边界后，发现 validator 对 manifest/context 的 `artifact_count` 只检查类型，不校验与 `artifacts.length` 一致。
- 第 43-44 轮发现 manifest entry validator 没要求 `schema_version`、`artifact_id`、`consumers`、`data_sensitivity`，消费者会拿到弱索引。
- 第 45-46 轮发现 `build-impact-facts.js` 已拒绝 `mode:headless` 缺 `base:<ref>`，但 `build-run-metadata.js` 仍可在缺 base 时写 metadata，可能留下无 diff scope 的半成品 run。
- 第 47 轮发现 schema inventory 测试单独读取 `issue.schema.json`，但没有明确区分 artifact schema 与 standalone issue schema，容易误用统一断言。
- 第 48-50 轮发现 validator 已收紧 manifest entry contract，但 `artifact-manifest.schema.json` 没同步 required 字段，存在 source schema 与 runtime validator 漂移。

具体修复细节：

- `extract-figma-contract.js`
  - 引入共享 `redactForArtifactText()`。
  - 新增 `isSensitiveArtifactText()`，只要共享脱敏器会改变文本，就不保留 Figma raw label/text。
  - `redactedLabelFields()`、`redactedTextFields()`、`shouldRetainShortLabel()` 都统一走该敏感文本判断。
- `validate-artifacts.js`
  - 对 `figma-design-contract` 递归校验 `name`、`raw_label`、`text`，拒绝 raw URL、Authorization、Cookie、secret-bearing text。
  - 对 strict issue 增加 `provenance`、`evidence`、`runtime_verification`、`affected_surface.file` 的脱敏与 public path 校验。
  - 新增 `artifact_path_not_public`，拒绝绝对路径作为最终 artifact path-like 字段。
  - 对 `app-audit-context` 和 `artifact-manifest` 校验 `artifact_count === artifacts.length`。
  - 对 manifest entry 要求 `path`、`schema_version`、`artifact_id`、`producer`、`consumers`、`sha256`、`freshness`、`data_sensitivity`、`contract_status`。
- `merge-contracts.js`
  - `gateInputIssues()` 向 `applyEvidenceGate()` / `normalizeIssue()` 传递 `repoRoot` / `source`。
  - `normalizeIssue()` 增加 `affected_surface` path-like 字段规范化。
  - evidence/provenance 的 `file` / `path` 字段通过 `sanitizePathLike()` 转为 repo-relative 或 `<issue-outside-repo:...>` / `<issue-path:redacted>`。
  - evidence/provenance/runtime reason 继续统一走共享脱敏器。
- `render-headless-envelope.js`
  - 对 `Artifact`、`Summary`、`Issues` 三个 path line 使用 `safePathLine()`。
  - 绝对路径统一输出 `<absolute-path:redacted>`。
- `build-artifact-manifest.js`
  - manifest 构建排除 `latest-summary.json`，与 `build-audit-context.js` 的事实源排除策略对齐。
- `build-run-metadata.js`
  - `mode:headless` 缺少 `base:<ref>` 时，在写 metadata 前失败，错误码为 `scope_headless_missing_base`。
- `artifact-manifest.schema.json`
  - 同步 manifest entry required 字段，避免 schema 与 validator 漂移。
- 测试补充：
  - internal Figma redaction 拒绝 URL / Authorization / Cookie raw label/text。
  - validator 拒绝未脱敏 Figma contract raw text。
  - headless envelope 不泄漏 metadata 绝对路径。
  - strict issue validator 拒绝 evidence/provenance/runtime reason 中的 secret text 和绝对路径。
  - report assembler 会把 issue path-like 字段规范成 repo-relative，并通过 strict validation。
  - manifest 排除 `latest-summary.json`。
  - metadata builder 在 headless 缺 base 时不写 output。
  - manifest/context `artifact_count` drift 检测。
  - manifest entry consumer-facing metadata 必填。
  - schema inventory 明确 artifact schema 与 standalone issue schema。

修复结论：

- 第 20-50 轮小步审查已完成。
- 本批修复继续保持轻契约：脚本只做确定性脱敏、路径规范化、schema/validator 校验和 no-write/headless guard，不替代 LLM 做语义审查结论。
- source/runtime 边界未改变；未手改 `.claude/`、`.codex/`、`.agents/skills/` generated runtime。

修复效果：

- 已通过聚焦验证：

```text
npx jest tests/unit/spec-app-consistency-audit-contract-extraction.test.js --runInBand
npx jest tests/unit/spec-app-consistency-audit-validate.test.js tests/unit/spec-app-consistency-audit-report.test.js --runInBand
npx jest tests/unit/spec-app-consistency-audit-cli-e2e.test.js --runInBand
```

- 当前新增覆盖把 Figma raw input、issue evidence/provenance、headless envelope path、manifest pointer、metadata headless scope 和 manifest entry contract 纳入可重复回归。

### 2026-05-02 06:24 收口验证记录

问题描述：

- 第 20-50 轮修复完成后，需要用可重复命令确认 app-audit contract spine、schema、validator、entrypoint 和 runtime 边界没有破坏。
- GitNexus 对本次整体 diff 返回 `critical` 风险，需要记录原因与处理结论，避免误解为未处理阻断。

具体修复细节：

- 运行完整 app-audit 单测：

```text
npx jest tests/unit/spec-app-consistency-audit-*.test.js --runInBand

Test Suites: 17 passed, 17 total
Tests: 76 passed, 76 total
```

- 运行脚本语法检查：

```text
find skills/spec-app-consistency-audit/scripts -maxdepth 2 -name '*.js' -print | sort | xargs -I{} node --check {}
```

- 运行项目 typecheck：

```text
npm run typecheck

typecheck passed (34 files checked)
```

- 运行 skill entrypoint lint：

```text
npm run lint:skill-entrypoints

skill entrypoint lint passed (161 files scanned)
```

- 运行 whitespace diff 检查：

```text
git diff --check
```

- 运行 schema parse 检查：

```text
schemas parsed: 22
```

- 检查 generated runtime diff：

```text
git diff --name-only -- .claude .codex .agents/skills

<no output>
```

- 运行 GitNexus 变更影响检测：

```text
mcp__gitnexus__.detect_changes(scope: all)

changed_count: 153
affected_count: 65
changed_files: 23
risk_level: critical
```

修复结论：

- 验证命令全部通过。
- GitNexus `critical` 与本次任务性质一致：这是跨 app-audit skill prose、prompts、scripts、schemas、tests、template 与 docs 的大型 contract upgrade，不是单点异常扩散。
- 旧 loop 文档删除仍是既有无关工作区状态，本任务不恢复、不回滚。
- `.claude/`、`.codex/`、`.agents/skills/` generated runtime 未被手改。

修复效果：

- 当前改造已具备可重复的单测、语法、typecheck、entrypoint、schema、diff 和影响面验证证据。
- 后续若继续提交或发 PR，应在说明中明确 GitNexus critical 来自预期的大范围 app-audit contract 改造，并列出上述验证命令。

### 2026-05-02 06:29 主测试链路追加验证

问题描述：

- 在 GitNexus 返回 `critical` 影响面后，仅跑 app-audit 聚焦测试仍不足以证明仓库主链路健康。
- 首次运行 `npm test` 时，`tests/unit/user-manual-contracts.test.js` 失败：`docs/05-用户手册/README.md` 的版本线仍是 `v1.6.1`，但 `package.json` 已是 `1.6.2`。

具体修复细节：

- 将 `docs/05-用户手册/README.md` 中 `当前版本线` 从 `v1.6.1` 同步为 `v1.6.2`。
- 追加 `CHANGELOG.md` 记录，说明该修复是主测试链路发现的用户手册契约漂移。
- 先单独重跑：

```text
npx jest tests/unit/user-manual-contracts.test.js --runInBand

Test Suites: 1 passed, 1 total
Tests: 2 passed, 2 total
```

- 再完整重跑：

```text
npm test

Test Suites: 77 passed, 77 total
Tests: 383 passed, 383 total
Smoke: passed
Integration: passed
E2E: passed
```

修复结论：

- `npm test` 已从失败恢复为完整通过。
- 用户手册版本线漂移属于相邻文档契约问题，已用最小 docs-only 修改修复。

修复效果：

- app-audit 大改造之外的主测试链路也已验证通过。
- `git diff --check` 复跑通过。
- `.claude/`、`.codex/`、`.agents/skills/` generated runtime 仍无变更。

### 2026-05-02 11:34 真实测试审查问题修复

问题描述：

- P1：真实测试审查发现 strict issue validator 只检查 `provenance.length > 0` 和 evidence bucket 非空，`provenance: [{}]` 与 `evidence: { code: [{}] }` 这类不可追溯空证据仍可通过最终 `audit-report` validation，削弱 `No evidence, no issue` 契约。
- P2：`industry_compliance` 的 evidence gate 只有未确认行业 profile 的拒绝路径测试，缺少 `confirmedIndustry` 正向路径测试。未来如果误改成所有 industry confirmed issue 都拒绝，现有测试无法发现。

具体修复细节：

- 在 `validate-artifacts.js` 中新增 strict evidence trace 校验：
  - 证据 entry 必须是对象。
  - `provenance[]` 与数组型 `evidence[]` 必须显式带 `source`。
  - provenance/evidence entry 必须至少包含 `file`、`path`、`artifact_id`、`node_id`、`route`、`event`、`key` 之一作为可追溯字段。
  - 继续保留既有脱敏和 public path 校验。
- 在 `tests/unit/spec-app-consistency-audit-validate.test.js` 增加空 provenance/evidence entry 拒绝测试，断言 `evidence_source_required` 与 `evidence_trace_required`。
- 在 `tests/unit/spec-app-consistency-audit-evidence.test.js` 增加 confirmed industry 正向测试，断言 `confirmedIndustry: finance-common` 时 `industry_compliance` confirmed issue 保持 accepted。
- 更新 `CHANGELOG.md` 记录本次 source/test 变更。

修复结论：

- P1 已修复：最终 report/issue artifact strict validation 不再接受不可追溯空证据。
- P2 已修复：industry confirmed 的允许路径已有回归测试保护。
- 本次修复仍保持轻契约：validator 只校验确定性结构、来源字段、追溯字段与脱敏，不替代 LLM 判断证据语义是否充分。

修复效果：

- 空证据 probe 从 `valid: true` 变为 `valid: false`，错误码包含 `evidence_source_required` 与 `evidence_trace_required`。
- 聚焦测试已通过：

```text
npx jest tests/unit/spec-app-consistency-audit-validate.test.js tests/unit/spec-app-consistency-audit-evidence.test.js --runInBand

Test Suites: 2 passed, 2 total
Tests: 25 passed, 25 total
```

- 扩大验证已通过：

```text
npx jest tests/unit/spec-app-consistency-audit-*.test.js --runInBand

Test Suites: 17 passed, 17 total
Tests: 78 passed, 78 total

npm run typecheck

typecheck passed (34 files checked)

npm test

Test Suites: 77 passed, 77 total
Tests: 385 passed, 385 total
Smoke: passed
Integration: passed
E2E: passed

git diff --check

<passed>

git diff --name-only -- .claude .codex .agents/skills

<no output>
```

- GitNexus 变更检测：

```text
mcp__gitnexus__.detect_changes(scope: all)

changed_count: 159
affected_count: 65
changed_files: 23
risk_level: critical
```

- 结论：`critical` 仍来自本轮 app-audit contract upgrade 的整体大范围影响面；本次追加修复只增加 strict issue evidence trace gate 与 industry confirmed 正向回归测试，没有手改 generated runtime。
