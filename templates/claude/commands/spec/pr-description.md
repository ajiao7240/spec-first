---
description: "Write or regenerate a value-first pull-request description"
argument-hint: "[PR ref or free-text steering]"
---

# Spec-First PR Description

This source template defines Claude command metadata only.

During `spec-first init --claude`, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-pr-description/SKILL.md`.

Edit the paired skill to change workflow behavior.
