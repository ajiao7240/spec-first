---
description: "[BETA] Run Spec-First execution with external delegate support"
argument-hint: "[plan or work description] [delegate:codex]"
---

# Spec-First Work Beta

This source template defines Claude command metadata only.

During `spec-first init --claude`, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-work-beta/SKILL.md`.

Edit the paired skill to change workflow behavior.
