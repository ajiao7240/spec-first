---
description: "Search Slack for interpreted organizational context"
argument-hint: "[topic or question]"
---

# Spec-First Slack Research

This source template defines Claude command metadata only.

During `spec-first init --claude`, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-slack-research/SKILL.md`.

Edit the paired skill to change workflow behavior.
