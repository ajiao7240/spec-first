---
description: "Build required external graph-provider indexes for spec-first workflows"
argument-hint: ""
---

# Graph Bootstrap

This source template defines Claude command metadata only.

During `spec-first init --claude`, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-graph-bootstrap/SKILL.md`.

Edit the paired skill to change workflow behavior.
