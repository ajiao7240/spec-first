---
description: "Run the Spec-First App consistency audit workflow"
argument-hint: "[prd path] [source path] [figma context or options]"
---

# Spec-First App Consistency Audit

This source template defines Claude command metadata only.

During `spec-first init --claude`, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-app-consistency-audit/SKILL.md`.

Edit the paired skill to change workflow behavior.
