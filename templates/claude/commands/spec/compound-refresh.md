---
description: "Refresh stale Spec-First solution docs"
argument-hint: "[scope hint or mode:autofix]"
---

# Spec-First Compound Refresh

This source template defines Claude command metadata only.

During `spec-first init` for Claude Code, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-compound-refresh/SKILL.md`.

Edit the paired skill to change workflow behavior.
