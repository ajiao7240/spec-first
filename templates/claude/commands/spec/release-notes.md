---
description: "Summarize recent spec-first releases or answer release questions"
argument-hint: "[optional release question]"
---

# Spec-First Release Notes

This source template defines Claude command metadata only.

During `spec-first init` for Claude Code, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-release-notes/SKILL.md`.

Edit the paired skill to change workflow behavior.
