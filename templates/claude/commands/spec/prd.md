---
description: "Run the Spec-First PRD requirements workflow"
argument-hint: "[increment request, existing PRD path, or validation target]"
---

# Spec-First PRD

This source template defines Claude command metadata only.

During `spec-first init` for Claude Code, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-prd/SKILL.md`.

Edit the paired skill to change workflow behavior.
