---
description: "Run metric-driven iterative optimization loops"
argument-hint: "[optimization spec YAML or goal]"
---

# Spec-First Optimize

This source template defines Claude command metadata only.

During `spec-first init` for Claude Code, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-optimize/SKILL.md`.

Edit the paired skill to change workflow behavior.
