---
description: "Run the Spec-First ideation workflow"
argument-hint: "[feature, focus area, or constraint]"
---

# Spec-First Ideate

This source template defines Claude command metadata only.

During `spec-first init` for Claude Code, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-ideate/SKILL.md`.

Edit the paired skill to change workflow behavior.
