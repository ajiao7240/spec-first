---
description: "Diagnose and configure the spec-first environment"
argument-hint: "[optional setup goal]"
---

# Spec-First Setup

This source template defines Claude command metadata only.

During `spec-first init --claude`, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-setup/SKILL.md`.

Edit the paired skill to change workflow behavior.
