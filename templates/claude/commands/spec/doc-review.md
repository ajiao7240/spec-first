---
description: "Run the Spec-First document review workflow"
argument-hint: "[mode:headless] [path/to/document.md]"
---

# Spec-First Document Review

This source template defines Claude command metadata only.

During `spec-first init --claude`, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-doc-review/SKILL.md`.

Edit the paired skill to change workflow behavior.
