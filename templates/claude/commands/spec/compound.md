---
description: "Run the Spec-First knowledge capture workflow"
argument-hint: "[brief problem context]"
---

# Spec-First Compound

This source template defines Claude command metadata only.

During `spec-first init --claude`, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-compound/SKILL.md`.

Edit the paired skill to change workflow behavior.
