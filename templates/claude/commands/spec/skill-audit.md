---
description: "Run the Spec-First skill audit workflow"
argument-hint: "[target skill path or audit options]"
---

# Spec-First Skill Audit

This source template defines Claude command metadata only.

During `spec-first init` for Claude Code, spec-first renders the runtime command by combining this frontmatter with the body of `skills/spec-skill-audit/SKILL.md`.

Edit the paired skill to change workflow behavior.
