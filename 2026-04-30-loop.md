# 2026-04-30 Spec-First 整体质量循环审查记录

执行时间：2026-04-30  
执行身份：Spec-First Evolution Architect  
隔离 worktree：`/Users/kuang/xiaobu/spec-first-loop-2026-04-30-2`  
分支：`audit/2026-04-30-loop-2`  
基线提交：`ac3041ec feat(cli): refresh startup version reminder flow`  

## 执行基线

- 已按仓库规则先读取 `docs/10-prompt/项目角色.md`，本轮判断基线为：轻 contract、明确边界、让 LLM 决策，脚本做确定性执行，LLM 做语义判断。
- 已按入口治理使用 `using-spec-first` 判断，本轮属于持续审查与修订，落到 `$spec-work` 执行姿势，不切换到 PR 前置的 `$spec-code-review`。
- 已在修改 `src/cli/commands/init.js` 前运行 GitNexus impact：`printInitNextSteps` 上游影响为 LOW，直接调用方为 `runInit`，受影响流程为 `runInit` / `runCli`。
- 本记录只记录本轮真实检查与修复；不是每个轮次都需要改文件，无问题轮次记录为审查结论。

## 100 轮小步循环记录

| 轮次 | 问题描述 | 具体修复细节 | 修复结论 | 修复效果 | 验证 |
|---:|---|---|---|---|---|
| 001 | 执行前需要校准 spec-first 演化角色，避免按单点功能思维处理。 | 读取 `docs/10-prompt/项目角色.md`，确认脚本/LLM 边界与轻 contract 原则。 | 无代码改动。 | 后续修复优先选择文档/contract/测试收口，不引入强编排。 | 人工阅读完成。 |
| 002 | 需要避免污染用户主 worktree。 | 沿用新建 worktree `/Users/kuang/xiaobu/spec-first-loop-2026-04-30-2` 和分支 `audit/2026-04-30-loop-2`。 | 无代码改动。 | 本轮改动与主工作区既有脏改隔离。 | `git status --short` 确认只在隔离 worktree 内工作。 |
| 003 | substantial work 前需要选择正确 workflow 入口。 | 读取 `using-spec-first` 与 `spec-work` 规则，采用 `$spec-work` 执行姿势。 | 无代码改动。 | 避免把本轮持续修复误路由为 brainstorm 或 PR review。 | 入口判断完成。 |
| 004 | 修改 `printInitNextSteps` 前需要确认影响面。 | 执行 `npx gitnexus impact printInitNextSteps --repo spec-first --direction upstream`。 | 风险 LOW，可继续。 | 明确影响仅在 init 输出链路。 | GitNexus impact 结果为 LOW。 |
| 005 | 英文 README 使用 `--lang zh`，但内容是英文入口文档。 | 将 `README.md` 的 init 示例改为 `--lang en`。 | 已修复。 | 英文用户复制命令后得到英文治理块和英文 init 下一步。 | `readme-language-split` 单测覆盖。 |
| 006 | `init --lang en` 实际仍输出中文“下一步”。 | `runInit` 调用 `printInitNextSteps(platform, developer.lang)`；`printInitNextSteps` 增加英文分支。 | 已修复。 | init 输出语言与项目 developer profile 对齐。 | `tests/unit/init-dry-run.test.js` 新增英文 Codex 用例。 |
| 007 | 英文 README 的 expected output 与修复后的 init 行为不一致。 | 将 Claude/Codex expected output 中“下一步”改为 `Next steps` 英文段落。 | 已修复。 | README 示例成为可验证的真实 CLI 输出。 | `tests/unit/readme-language-split.test.js` 覆盖。 |
| 008 | 中文 README 混入英文表述。 | 将 `required harness runtime setup`、`polish browser-visible UI beta`、`release notes` 等表格项本地化。 | 已修复。 | 中文 README 对中文用户更一致。 | targeted Jest 已覆盖 README 文案。 |
| 009 | `spec-best-practices-researcher` 仍把 `.codex/skills` 当当前 skill discovery。 | 改为只把 `.codex/skills` / `~/.codex/skills` 视为 legacy cleanup signals。 | 已修复。 | 研究 agent 不会把退役路径当 curated source。 | `best-practices-researcher-contracts.test.js` 覆盖正反断言。 |
| 010 | 部分 bundled agent Markdown 有尾随空白。 | 清理 `spec-code-simplicity-reviewer` 与 `spec-design-implementation-reviewer` 的尾随空白。 | 已修复。 | 减少无意义 diff 与 Markdown 噪音。 | `git diff --check` 通过。 |
| 011 | agent 尾随空白缺少回归保护。 | 在 `agent-support-contracts.test.js` 增加 bundled markdown agents 无尾随空白测试。 | 已修复。 | 未来 agent prose 变更会被单测拦截。 | targeted Jest 通过。 |
| 012 | 发布包可能重新带入 Python bytecode 缓存。 | `.npmignore` 增加 `__pycache__`、`*.pyc`、`*.pyo`。 | 已修复。 | 降低 npm tarball 污染风险。 | `npm pack --dry-run --json` 检查通过。 |
| 013 | `package.json files` 白名单模式下 `.npmignore` 之外还需显式排除。 | 在 `package.json` files 中加入 `!skills/**/__pycache__/**`、`!skills/**/*.pyc`、`!skills/**/*.pyo`。 | 已修复。 | package 白名单与 ignore 双保险。 | `package-install-contracts.test.js` 覆盖。 |
| 014 | smoke 测试没有验证 tarball 中 bytecode 缓存。 | 在 `tests/smoke/install-tarball.sh` 和 `release-dual-host-governance.sh` 加入 tarball bytecode grep guard。 | 已修复。 | 发布 smoke 会阻止缓存文件进入包。 | `bash -n` 通过。 |
| 015 | npm pack bytecode 排除缺少真实 fixture 验证。 | `package-install-contracts.test.js` 动态创建 `.pyc` fixture，执行 `npm pack --dry-run --json` 并断言不入包。 | 已修复。 | 证明排除规则真实生效，不只是静态断言。 | targeted Jest 通过，fixture 清理后无残留。 |
| 016 | `docs/项目审查` 索引含本机绝对路径链接。 | 将两个索引页转换为相对 Markdown 链接。 | 已修复。 | 审查文档离开本机后仍可阅读。 | 新增 `project-review-docs-contracts.test.js`。 |
| 017 | 项目审查目录更多正文仍有 `/Users/kuang/xiaobu/spec-first` 链接。 | 机械转换 `docs/项目审查/**/*.md` 中 spec-first 本仓绝对链接为相对链接或 repo-relative 路径。 | 已修复。 | 全目录内不再绑定本机 spec-first 路径。 | `rg '/Users/kuang/xiaobu/spec-first' docs/项目审查` 无命中。 |
| 018 | 项目审查文档引用外部本机 clone 路径。 | 将 `graphify`、`code-review-graph`、`superpowers` 的 `/Users/kuang/...` 链接改为代码样式路径。 | 已修复。 | 文档不再生成不可点击的机器本地链接。 | `rg '/Users/kuang|file://' docs/项目审查` 无命中。 |
| 019 | 项目审查路径转换后产生 Markdown 尾随硬换行。 | 对本轮改动的 `docs/项目审查` 文件清理尾随空白。 | 已修复。 | `git diff --check` 不再失败。 | `git diff --check` 通过。 |
| 020 | 用户手册快速开始仍要求检查 Codex `.codex/commands/spec`。 | 将 Codex 验证项收口为 `.agents/skills` 与 `.codex/agents`。 | 已修复。 | 新用户不会按旧 slash command 目录判断失败。 | `dual-host-governance-contracts.test.js` 覆盖。 |
| 021 | 本地源码安装指南仍 `ls .codex/commands/spec`。 | 删除 Codex 命令目录检查，只保留 `.agents/skills`。 | 已修复。 | 本地安装验证与当前 runtime 模型一致。 | `dual-host-governance-contracts.test.js` 覆盖。 |
| 022 | FAQ 标题把 Codex 也描述为 `/spec:*` 入口。 | 改为“Claude 的 `/spec:*` 或 Codex 的 `$spec-*`”。 | 已修复。 | 排障入口表述清晰区分双宿主。 | `dual-host-governance-contracts.test.js` 覆盖。 |
| 023 | 版本更新页旧条目仍写“Codex init 现在也会生成 `/spec:*`”。 | 将 2026-04-08 Codex 条目标注为历史短暂兼容层，补充当前已撤回状态。 | 已修复。 | Release notes 不再误导当前用户。 | `dual-host-governance-contracts.test.js` 覆盖。 |
| 024 | 用户手册首页版本线停留在 `v1.5.9`。 | 更新为 `v1.6.1`。 | 已修复。 | 手册首页与 package version 对齐。 | 新增 `user-manual-contracts.test.js`。 |
| 025 | 用户手册版本线缺少回归保护。 | `user-manual-contracts.test.js` 读取 `package.json` 并断言手册版本线。 | 已修复。 | 后续版本 bump 若漏改手册会失败。 | targeted Jest 通过。 |
| 026 | 贡献者指南仍把 `.codex/skills` 写作运行时路径。 | 改为 `.agents/skills`，并在 checklist 中强调不要把 runtime 目录当源码真源。 | 已修复。 | 新增 skill 时不会回到旧 Codex path。 | `contributor-guides-contracts.test.js` 覆盖。 |
| 027 | 新增 skill 操作清单仍称核心 workflow 会生成 `.codex/commands/spec/<name>.md`。 | 改为“不再生成”，Codex discovery 以 `.agents/skills/spec-<name>/` 为准。 | 已修复。 | 防止新 workflow 重新设计 compatibility layer。 | `contributor-guides-contracts.test.js` 覆盖。 |
| 028 | Codex 打包经验总结把 `.codex/commands/spec` 当兼容命令层当前产物。 | 改为旧命令层清理目标，正式 discovery 为 `$spec-*`。 | 已修复。 | 经验文档从“兼容生成”更新为“遗留清理”。 | `contributor-guides-contracts.test.js` 覆盖。 |
| 029 | 贡献者指南仍把 `.claude-plugin/plugin.json` 当 manifest 真源。 | 替换为 `src/cli/contracts/dual-host-governance/skills-governance.json`。 | 已修复。 | 新能力接入从退役 plugin manifest 转到治理 JSON。 | `contributor-guides-contracts.test.js` 覆盖。 |
| 030 | 新增 workflow 步骤仍要求修改 `.claude-plugin/plugin.json`。 | 改为更新 `skills-governance.json` 并新增 Claude command template。 | 已修复。 | 贡献者按当前 CLI runtime 生成链路操作。 | `contributor-guides-contracts.test.js` 覆盖。 |
| 031 | README runtime 资产数量可能漂移。 | 通过 `listBundledSkills`、`listBundledAgents`、`buildFilteredAssetSet` 计算数量。 | 无需改动。 | README 中 `39 skills / 51 agents / 18 workflows` 与代码一致。 | Node 计算结果与 README/test 一致。 |
| 032 | Codex init 输出是否仍包含 `$spec-* skills` 需要确认。 | 检查 `src/cli/commands/init.js` 输出逻辑。 | 无需额外改动。 | Codex 使用 `$spec-* skills`，Claude 使用 `/spec:* commands`。 | `init-dry-run` 单测通过。 |
| 033 | 中文 README 是否还残留英文 `Next steps` 之外的错配。 | 扫描 `README.zh-CN.md` 和相关单测。 | 无需额外改动。 | 中文 README 继续保留中文 init 输出，英文 README 保留英文输出。 | `readme-language-split` 通过。 |
| 034 | 英文 README 是否仍含中文“下一步”。 | 添加并运行英文 README 负向断言。 | 已修复。 | 英文 README 不再混入中文 init 输出。 | `readme-language-split` 通过。 |
| 035 | 用户手册当前事实页是否还出现退役 `spec-setup`。 | 扫描 `docs/05-用户手册`、README、skills、agents、src。 | 无需改动。 | 命中仅为退役测试或合法说明。 | `rg '/spec:setup|$spec-setup|spec-setup'` 审查完成。 |
| 036 | 用户手册是否还把 Codex 写成 slash 入口。 | 扫描 `Codex.*?/spec:` 与 `/spec:.*Codex`。 | 无需额外改动。 | 剩余命中为合法双宿主表述或“误区”说明。 | `rg` 审查完成。 |
| 037 | `docs/10-prompt` 是否把旧 runtime path 当现行真源。 | 扫描 `.claude-plugin`、`.codex/skills`、`.codex/commands/spec`。 | 无需改动。 | 命中为审查主题或 legacy cleanup 说明，不是现行指令。 | `rg` 审查完成。 |
| 038 | `write-tasks` 是否被误写为 command-backed workflow。 | 扫描 `$spec-write-tasks` 与 `/spec:write-tasks`。 | 无需改动。 | `using-spec-first` 与 `plan-handoff` 均明确 standalone skill。 | `rg` 审查完成。 |
| 039 | generated runtime mirrors 是否被手动修改。 | 审查状态中未出现 `.claude/`、`.codex/`、`.agents/skills/` 源外生成态修改。 | 无需改动。 | 遵守 source-of-truth 边界。 | `git status --short` 检查。 |
| 040 | 发布包是否有真实 `.pyc` 残留。 | 执行 `find skills -path '*/__pycache__/*' -o -name '*.pyc' -o -name '*.pyo'`。 | 无需改动。 | 当前 worktree 无 bytecode 残留。 | find 无输出。 |
| 041 | tracked 文件是否已有 pycache/bytecode。 | 执行 `git ls-files | rg '(^|/)__pycache__/|\\.py[co]$'`。 | 无需改动。 | git index 未跟踪 bytecode。 | rg 无命中。 |
| 042 | shell smoke 新增语法是否有效。 | 对两个 smoke 脚本执行 `bash -n`。 | 无需改动。 | shell 语法通过。 | `bash -n` 通过。 |
| 043 | skill entrypoint/frontmatter 是否被本轮文档改动影响。 | 运行 `npm run lint:skill-entrypoints`。 | 无需改动。 | 125 个文件扫描通过。 | lint 通过。 |
| 044 | JS/CJS 语法是否受 init/test 改动影响。 | 运行 `npm run typecheck`。 | 无需改动。 | 33 个 JS/CJS 文件语法通过。 | typecheck 通过。 |
| 045 | 本轮 diff 是否含尾随空白。 | 运行 `git diff --check`。 | 无需改动。 | 当前 diff whitespace clean。 | `git diff --check` 通过。 |
| 046 | npm pack 是否仍带 `__pycache__` 或 `.pyc/.pyo`。 | 执行 `npm pack --dry-run --json` 并过滤路径。 | 无需改动。 | pack 文件数 302，无 bytecode。 | 输出 `pack-clean 302`。 |
| 047 | init 英文行为与 README 示例是否一起验证。 | targeted Jest 覆盖 `init-dry-run` 与 `readme-language-split`。 | 无需额外改动。 | CLI 行为与文档示例同步。 | 8 个 targeted suites 通过。 |
| 048 | 双宿主治理文档是否覆盖用户手册和 release notes。 | 扩展 `dual-host-governance-contracts.test.js`。 | 已修复。 | 入口漂移从 README 扩展防护到手册/版本页。 | targeted Jest 通过。 |
| 049 | 项目审查 portable link contract 是否过窄。 | 将测试从两个 index 扩到 `docs/项目审查/**/*.md`。 | 已修复。 | 全审查目录禁止机器本地链接。 | `project-review-docs-contracts.test.js` 通过。 |
| 050 | contributor guide contract 是否覆盖 Codex 和 manifest 双漂移。 | 新增 `contributor-guides-contracts.test.js`。 | 已修复。 | 退役 `.codex/skills`、`.claude-plugin`、旧命令生成说法被拦截。 | targeted Jest 通过。 |
| 051 | package install contract 是否覆盖 `.npmignore`。 | `package-install-contracts.test.js` 读取 `.npmignore` 并断言 bytecode 忽略规则。 | 已修复。 | ignore 和 package files 同步受测。 | targeted Jest 通过。 |
| 052 | smoke tarball guard 是否和 unit pack guard 重复但有价值。 | 保留 unit 真 fixture + smoke tarball list 双层验证。 | 无需改动。 | 单元快、smoke 贴近发布链路。 | unit 与 `bash -n` 通过。 |
| 053 | `README.zh-CN.md` 是否存在命令入口表的 Codex slash 漂移。 | 扫描 README 双语入口表。 | 无需改动。 | 表格仍为 Claude `/spec:*`、Codex `$spec-*`。 | `rg` 审查完成。 |
| 054 | `src/cli/index.js` 顶层重启提示是否错误。 | 检查命中“Claude 的 /spec:* 或 Codex 的 $spec-*”。 | 无需改动。 | 这是合法双宿主提示。 | `rg` 审查完成。 |
| 055 | best-practices researcher 是否仍保留旧 `.codex/skills` 作为当前路径。 | 检查 agent 正文和测试断言。 | 无需额外改动。 | 旧路径只作为 legacy cleanup signals。 | targeted Jest 通过。 |
| 056 | `docs/05-用户手册/README.md` 是否仍提醒以首页事实为准。 | 检查 Stage-0 旧目录提示。 | 无需改动。 | 该提示用于处理历史章节，不是现行入口。 | `rg` 审查完成。 |
| 057 | release notes 历史 `.claude-plugin` 命中是否需要改。 | 判断其处于历史版本记录，不作为当前贡献指南。 | 无需改动。 | 保留历史脉络，当前指南另有 contract。 | `rg` 审查完成。 |
| 058 | `docs/03-实施方案` active guide 是否仍有退役 manifest。 | 修复后复扫 active guide。 | 已修复。 | 无 `.claude-plugin/plugin.json` 命中。 | `rg` 只命中测试负向断言。 |
| 059 | `docs/07-经验总结` active checklist 是否仍有 `.codex/skills/`。 | 修复后复扫三份 active guide。 | 已修复。 | 无 retired `.codex/skills/` 命中。 | `rg` 只命中测试负向断言。 |
| 060 | agent support files 计数是否与 README 一致。 | 用 `buildFilteredAssetSet` 计算 support 为 0。 | 无需改动。 | README “no agent support files / 0 个”正确。 | Node 计算结果。 |
| 061 | Codex runtime commands 数量是否错误显示。 | 计算 Codex `commands: 0`、`workflowSkills: 18`。 | 无需改动。 | README 只写 Codex workflow skills，不写 commands。 | Node 计算结果。 |
| 062 | Claude runtime workflow copies 是否影响 README 表述。 | 计算 Claude `commands:18`、`workflows:18`、`skills:2`、`internalSkills:2`。 | 无需改动。 | README 中 Claude commands 和 skill directory 说明匹配。 | Node 计算结果。 |
| 063 | project review docs 机械改写是否误留外部绝对路径。 | 复扫 `/Users/kuang|file://`。 | 无需改动。 | 全目录无机器本地链接。 | `rg` 无命中。 |
| 064 | project review docs 相对链接是否肉眼可读。 | 抽查 principle rereview 和 graphify integration 前段。 | 无需改动。 | 相对链接与外部代码路径表述可读。 | `sed` 抽查完成。 |
| 065 | init 输出中文默认行为是否保持。 | `printInitNextSteps` 保留 default `zh` 分支。 | 无需额外改动。 | 未传 `--lang en` 时行为不变。 | `init-dry-run` 中文用例通过。 |
| 066 | init 英文分支是否影响 Claude。 | README 期望 Claude 英文示例，函数按 platform 输出 Claude commands。 | 无需额外改动。 | Claude 英文输出路径正确。 | README contract 覆盖。 |
| 067 | init 英文分支是否影响 Codex。 | 新增 Codex `--lang en` apply 测试。 | 已修复。 | Codex 英文输出 `$spec-*`。 | `init-dry-run` 通过。 |
| 068 | package pack fixture 测试是否会留下 `__pycache__`。 | `finally` 中 `fs.rmSync(cacheDir)` 清理 fixture。 | 无需改动。 | 测试后 worktree 无 bytecode 残留。 | find 无输出。 |
| 069 | smoke 脚本 tar list guard 是否定位过宽。 | grep 模式 `(^|/)__pycache__/|\\.py[co]$` 只命中 bytecode 缓存。 | 无需改动。 | 不会误伤普通 Python 源文件。 | `bash -n` 与 pack check 通过。 |
| 070 | Changelog 治理要求是否需要执行。 | 在 `CHANGELOG.md` 顶部追加 `v1.6.1 2026-04-30 13:36:13 leokuang` 记录。 | 已修复。 | 本轮源码/文档/test 改动符合仓库 changelog 治理。 | `CHANGELOG.md` 已更新。 |
| 071 | 当前修改是否触碰生成态 runtime。 | 状态未出现 `.claude/`、`.codex/`、`.agents/skills/` 生成资产修改。 | 无需改动。 | 避免 source/runtime drift。 | `git status --short`。 |
| 072 | `docs/05-用户手册/06-本地源码安装.md` 开头模型是否当前。 | 检查其说明 Claude `/spec:*` 与 Codex `$spec-*`。 | 无需改动。 | 和当前运行模型一致。 | `sed` 抽查完成。 |
| 073 | FAQ 是否仍有 Codex slash 问题标题。 | 修复后复扫 FAQ 标题。 | 已修复。 | FAQ 标题准确。 | `dual-host-governance-contracts` 通过。 |
| 074 | Release notes 是否仍含精确旧句“Codex init 现在也会生成”。 | 增加负向断言。 | 已修复。 | 当前版本页不会传播旧句。 | `dual-host-governance-contracts` 通过。 |
| 075 | 项目审查 docs 测试是否会误伤外部路径文本。 | 将机器本地路径禁用为 `/Users/kuang`，外部 repo 仅保留相对代码文本。 | 已修复。 | contract 关注可移植性，不禁止普通相对代码路径。 | `project-review-docs-contracts` 通过。 |
| 076 | 当前 docs/项目审查 是否还有 `file://`。 | 复扫全目录。 | 无需改动。 | 无 file URI。 | `rg` 无命中。 |
| 077 | 全量 unit 是否受新增 contract 影响。 | 运行 `npm run test:unit`。 | 无需改动。 | 58 suites / 260 tests 全部通过。 | `npm run test:unit` 通过。 |
| 078 | unit 后是否产生 bytecode 缓存。 | unit 后再次 find `skills/**/__pycache__` 与 `.pyc/.pyo`。 | 无需改动。 | 测试没有留下生成垃圾。 | find 无输出。 |
| 079 | unit 后 diff whitespace 是否仍 clean。 | unit 后再次 `git diff --check`。 | 无需改动。 | diff 仍无 whitespace 错误。 | `git diff --check` 通过。 |
| 080 | skill lint 是否覆盖新增 contributor docs。 | contributor docs 非 skill，但 lint 覆盖 skills/agents/templates 没有回归。 | 无需改动。 | prompt 资产入口仍有效。 | `lint:skill-entrypoints` 通过。 |
| 081 | `docs/08` 历史记录是否应全量重写。 | 判断仅更新会误导当前用户的 Codex entrypoint 条目，不机械改历史所有 `.claude-plugin`。 | 无需额外改动。 | 保留历史语境，避免过度清理。 | 人工审查。 |
| 082 | 是否需要引入新的抽象处理 init 多语言。 | 采用最小分支，不新增 i18n 框架。 | 已修复。 | 符合轻 contract 与局部最小改动。 | 代码 diff 审查。 |
| 083 | 是否需要为中文 README 新增测试。 | 现有 dual-host/readme tests 已覆盖中文关键表述。 | 无需新文件。 | 避免重复 contract。 | targeted Jest 通过。 |
| 084 | `CHANGELOG.md` 作者来源是否明确。 | 读取主项目 `.codex/spec-first/.developer`，确认 `name=leokuang`。 | 已修复。 | Changelog 作者使用项目级 developer profile。 | `cat /Users/kuang/xiaobu/spec-first/.codex/spec-first/.developer`。 |
| 085 | 本轮是否触碰函数以外 symbol。 | 只有 `printInitNextSteps` 行为变更，影响分析已覆盖。 | 无需额外 GitNexus impact。 | 遵守编辑前影响分析规则。 | GitNexus impact 已完成。 |
| 086 | docs 机械路径改写是否属于 symbol 修改。 | 仅文档链接，不涉及函数/类/方法 symbol。 | 无需 GitNexus impact。 | 避免滥用 impact。 | 人工判断。 |
| 087 | 新增 Jest 文件是否符合 CommonJS 风格。 | 新测试均使用 `'use strict'`、CommonJS、2 空格。 | 无需改动。 | 与仓库测试风格一致。 | `npm run typecheck` 与 unit 通过。 |
| 088 | `package-install-contracts` 是否会因 npm pack stdout 解析失败。 | 使用 `JSON.parse(result.stdout)`，目标命令固定 `--json`。 | 无需改动。 | 测试稳定读取 pack 文件列表。 | unit 通过。 |
| 089 | `project-review-docs-contracts` 递归文件列表是否稳定。 | 使用 `fs.readdirSync(..., { withFileTypes: true })` 递归，只读 `.md`。 | 无需改动。 | 新文档自动纳入 portable link contract。 | unit 通过。 |
| 090 | `contributor-guides-contracts` 是否误扫历史长档。 | 只扫描三份 active contributor guide/经验清单。 | 无需改动。 | 历史分析文档不会被过宽规则误伤。 | unit 通过。 |
| 091 | `dual-host-governance-contracts` 是否过度耦合 release notes。 | 只断言当前状态说明与旧精确句不存在。 | 无需改动。 | 防当前用户误读，不要求重写历史。 | unit 通过。 |
| 092 | `user-manual-contracts` 是否需自动推导路径。 | 直接读取 package version 与手册首页。 | 无需改动。 | 简单确定性 contract，维护成本低。 | unit 通过。 |
| 093 | `README.md` 是否仍指向中文 release notes。 | 英文 README 保留 Chinese-first docs 标识。 | 无需改动。 | 英文用户知道深入文档为中文优先。 | `readme-language-split` 通过。 |
| 094 | `docs/05-用户手册` 是否有旧 `.codex/commands/spec` 正向检查。 | 修复后剩余命中均为“不生成”说明。 | 无需改动。 | 不再把 legacy command dir 当安装成功条件。 | `rg` 审查完成。 |
| 095 | `docs/07` Codex 经验是否仍把 `.codex/commands/spec/brainstorm.md` 当存在条件。 | 改为不存在或已被 clean/init 清理。 | 已修复。 | 发布验收不会要求旧命令文件。 | contributor guide contract + 人工审查。 |
| 096 | 现有测试是否覆盖 release tarball bytecode 排除的真实行为。 | unit 真实 fixture + direct pack-clean command 双验证。 | 无需改动。 | bytecode 排除有实际证据。 | `pack-clean 302`。 |
| 097 | 当前 worktree 是否仍有意外未跟踪生成文件。 | 状态只显示新增测试和 loop 文件待加入，未见临时目录。 | 无需改动。 | 工作区可审查。 | `git status --short`。 |
| 098 | 是否需要重建 `.claude/.codex` runtime 验证 prose。 | 本轮修改 source docs/skills/tests，未手改 runtime；同会话 runtime 可能缓存，优先源码与 contract 验证。 | 无需重建。 | 遵守 Agent 与 Skill 变更验证规则。 | 源码读取 + unit/lint。 |
| 099 | 是否已经完成最终发布级验证。 | 继续执行 `npm run build`、`npm run test:smoke`、`npm run test:integration`、`npm run test:release`、`npx gitnexus detect-changes --repo spec-first`。 | 已完成。 | 发布包、安装态、双宿主 runtime、端到端流程和 GitNexus 变更影响均完成验证。 | build/smoke/integration/release/detect-changes 均通过；GitNexus 风险 medium。 |
| 100 | 本轮总体修复是否符合项目方向。 | 修复集中在输入质量、边界清晰度、source-of-truth 和发布包卫生；未新增强编排或状态机。 | 当前方向正确。 | spec-first 自身演化质量、用户安装路径和贡献者输入质量提升。 | 已通过 targeted Jest、lint、typecheck、pack、unit。 |

## 当前验证状态

- 已通过：`npx jest tests/unit/init-dry-run.test.js tests/unit/readme-language-split.test.js tests/unit/best-practices-researcher-contracts.test.js tests/unit/agent-support-contracts.test.js tests/unit/project-review-docs-contracts.test.js tests/unit/package-install-contracts.test.js tests/unit/dual-host-governance-contracts.test.js tests/unit/user-manual-contracts.test.js --runInBand`
- 已通过：`npx jest tests/unit/contributor-guides-contracts.test.js tests/unit/user-manual-contracts.test.js tests/unit/project-review-docs-contracts.test.js tests/unit/dual-host-governance-contracts.test.js --runInBand`
- 已通过：`npm run lint:skill-entrypoints`
- 已通过：`npm run typecheck`
- 已通过：`git diff --check`
- 已通过：`npm pack --dry-run --json` bytecode 过滤检查，结果 `pack-clean 302`
- 已通过：`bash -n tests/smoke/install-tarball.sh tests/smoke/release-dual-host-governance.sh`
- 已通过：`npm run test:unit`，结果 `58 passed / 260 tests`
- 已通过：`npm run build`
- 已通过：`npm run test:smoke`
- 已通过：`npm run test:integration`
- 已通过：`npm run test:release`
- 已通过：`npx gitnexus detect-changes --repo spec-first`，结果为 13 files / 21 symbols / 2 affected processes / medium risk；受影响流程集中在 `InspectSkillIntegrity` 与 `InspectAgents` 的 Codex path rewrite 检查。
- 已通过：最终 `git diff --check`
- 已通过：最终 bytecode 残留检查，`find skills -path '*/__pycache__/*' -o -name '*.pyc' -o -name '*.pyo'` 无输出。

## 最终收口结论

本轮 100 轮小步循环已完成。核心修复集中在四类：

- 输入质量：英文 init 输出、README 示例、用户手册版本线、贡献者指南真相源。
- 边界清晰度：Codex `$spec-*` 与 Claude `/spec:*` 的用户入口、旧 `.codex/commands/spec` 清理目标、退役 `.claude-plugin` manifest。
- 知识复用：项目审查文档链接可移植，循环记录沉淀可追溯。
- 工程落地：bytecode 发布包防护、smoke/release/unit contract 覆盖。
