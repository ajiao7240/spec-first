# Boundary Discipline Rubric

Good skills own one responsibility.

Check:

- Does the workflow take over upstream planning work?
- Does it perform downstream implementation without confirmation?
- Does it mix facts, standards, and review conclusions?
- Does it write source or runtime files by default?
- Does it specify handoff points when another workflow owns the next step?

Overlap candidates are not findings until the LLM confirms responsibility conflict.
