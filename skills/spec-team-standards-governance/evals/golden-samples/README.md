# Golden Samples

V2 可在后续加入小型 backend/frontend/app 样本，验证从 acquisition task pack 到 candidates、lineage、owner queue、retrieval eval 的端到端闭环。

当前没有可复现 PR/review 样本，因此本目录只记录合同边界：不得用空样本声明 replay pass，样本不足时使用 `not-enough-sample` 或 `not-run`。
