# Golden Samples

V2 may later add small backend/frontend/app samples to validate the end-to-end loop from acquisition task pack to candidates, lineage, owner queue and retrieval eval.

There are no reproducible PR/review samples yet, so this directory only records the contract boundary: do not claim replay pass from empty samples; use `not-enough-sample` or `not-run` when samples are insufficient.
