# spec-first 代码质量小步迭代记录

- 第 1 轮：发现 `spec-first init --force` 被解析器接受但没有实现和文档契约，修订为未知参数失败，并补 `init` 单测防止无效 CLI 面再次静默出现。验证目标：`npx jest tests/unit/init-dry-run.test.js --runInBand`。
- 第 2 轮：发现 `npm run typecheck` 只检查少数入口和脚本，漏掉 `src/cli/**` 主实现。新增 `scripts/typecheck-js.js`，让 typecheck 覆盖 `bin/`、`src/`、`scripts/` 的全部 JS/CJS 文件，并用 package contract 测试锁定覆盖范围。验证目标：`npm run typecheck` 与 `npx jest tests/unit/package-install-contracts.test.js --runInBand`。
- 第 3 轮：发现 `doctor` 运行时读取 `docs/contracts/verifiers/verification-evidence.schema.json`，但发布包未包含该 docs-side runtime schema。将 `docs/contracts/verifiers/` 纳入 `package.json files`，并在 release smoke 中断言 tarball 包含该 schema。验证目标：`npx jest tests/unit/package-install-contracts.test.js --runInBand` 与 `npm run build`。
- 第 4 轮：发现 task pack 的 `files` 契约没有显式拒绝反斜杠路径，可能导致 POSIX/Windows 下同一字符串语义不同。收紧 `isConcreteRepoRelativeFile`，要求 task file 使用 POSIX repo-relative separators，并补 validator 单测。验证目标：`npx jest tests/unit/task-pack-command.test.js --runInBand`。
- 第 5 轮：发现 `source_plan` frontmatter 的路径校验弱于 task `files`，会先尝试解析非 concrete 路径再报告 missing/outside。复用 concrete repo-relative 判断，统一要求 POSIX file path，并更新绝对路径与反斜杠路径测试。验证目标：`npx jest tests/unit/task-pack-command.test.js --runInBand`。
- 第 6 轮：发现 task pack optional `parallelizable` 字段缺少类型校验，字符串值会进入 deterministic handoff 并给执行策略留下歧义。新增 boolean-only 校验和单测，保持脚本只做边界类型判断、并不替 LLM 决策并行策略。验证目标：`npx jest tests/unit/task-pack-command.test.js --runInBand`。
- 第 7 轮：发现 `execution_waves[].wave` 没有限制为 string/number，对象会被字符串化后参与波次映射。新增 wave id 类型校验，防止结构错误被转换成看似合法的图节点。验证目标：`npx jest tests/unit/task-pack-command.test.js --runInBand`。
- 第 8 轮：发现 `spec-first tasks hash/validate` 会静默忽略未知 flags。新增 unknown option 检查，并保持 `--json` 错误输出可解析，避免 handoff 工具因拼写错误继续执行。验证目标：`npx jest tests/unit/task-pack-command.test.js --runInBand`。
- 第 9 轮：发现 `tasks validate --repo` 缺少路径值时会静默回退到 cwd。将 repo 参数解析改为显式结果对象，缺值返回 `tasks-repo-path-required`，避免用户误以为指定 repo 已生效。验证目标：`npx jest tests/unit/task-pack-command.test.js --runInBand`。
- 第 10 轮：发现 `package.json` 的 `typecheck` 指向新增脚本，但发布白名单未包含该脚本，导致发布包内 npm script 指向缺失文件。将 `scripts/typecheck-js.js` 纳入发布包，并补 package/release smoke 契约。验证目标：`npx jest tests/unit/package-install-contracts.test.js --runInBand` 与 `npm run build`。
- 第 11 轮：复核 `task-pack-schema.md` 的 JSON 示例字段唯一性，补 contract 测试锁定示例不得重复声明 `done_signal`，避免 LLM 从 schema 示例复制重复 JSON key。验证目标：`npx jest tests/unit/spec-write-tasks-contracts.test.js --runInBand`。
- 第 12 轮：发现 validator 已收紧 POSIX path、wave id 和 `parallelizable` 类型，但 schema 文案仍偏宽。更新 `task-pack-schema.md`，让 LLM 生成端和脚本校验端对齐同一轻 contract。验证目标：`npx jest tests/unit/spec-write-tasks-contracts.test.js --runInBand`。
- 第 13 轮：发现 `spec-work` / `spec-work-beta` 执行端只要求跑 validator，但没明确结构字段错误不可临场修补。补充执行器边界：非 POSIX 路径、非法 wave id、非 boolean `parallelizable` 等 validator 拒绝项是硬 handoff failure，应回到任务包生成。验证目标：`npx jest tests/unit/spec-write-tasks-contracts.test.js --runInBand`。
- 第 14 轮：发现 `spec-code-review` 在 Codex 上举了具体 mid-tier 模型名示例，容易随模型发布节奏漂移。改为只允许 host-provided stable alias，否则继承默认模型，并补 contract 测试禁止硬编码非 Claude 模型名。验证目标：`npx jest tests/unit/spec-code-review-contracts.test.js --runInBand`。
- 第 15 轮：发现 `spec-mcp-setup` 的本地配置模板仍用具体 `gpt-5.4` 作为 Codex delegation 模型示例，和执行端“未配置则继承用户 Codex 默认”的轻 contract 相冲突。改为 `<codex-model>` 占位，并补 contract 测试禁止模板重新 pin 具体模型名。验证目标：`npx jest tests/unit/spec-work-beta-contracts.test.js --runInBand`。
- 第 16 轮：继续复核模型治理，发现 `spec-code-review` 的 trivial PR 预判仍写着 Codex 具体模型示例 `gpt-5.4-nano`。将其改为“非 Claude 平台使用 host-provided cheap stable alias 或省略模型参数”，并补测试禁止该硬编码回归。验证目标：`npx jest tests/unit/spec-code-review-contracts.test.js --runInBand`。
- 第 17 轮：发现 `agent-native-architecture` 参考文档中的测试与模型分层示例仍写死 dated Claude model id，容易被用户复制为新项目默认配置。改为 `Config.models.*` / 环境变量式 host 配置别名，并补 contract 测试禁止 `claude-3-*` 与 dated Sonnet/Opus id 回流。验证目标：`npx jest tests/unit/agent-native-architecture-contracts.test.js --runInBand`。
- 第 18 轮：发现 `spec-mcp-setup` 配置模板的 `work_delegate_effort` 仍以 `high` 为示例，且顶部笼统写 invalid values 回退 defaults，容易误读为 spec-first 自带默认 effort。改为 `<effort>` 占位，并澄清 invalid config 按字段忽略或回退。验证目标：`npx jest tests/unit/spec-work-beta-contracts.test.js --runInBand`。
- 第 19 轮：复核 `using-spec-first` source/runtime surface 文案，当前源码已是 `init --claude` / `init --codex` 双宿主口径，但缺少防回归 contract。补测试锁定 managed bootstrap 与 runtime repair 两处必须同时提到 Claude/Codex，避免重新退化为 Codex-only 文案。验证目标：`npx jest tests/unit/using-spec-first-contracts.test.js --runInBand`。
- 第 20 轮：问题描述：`spec-plan/references/plan-handoff.md` 在开始 work、任务包 handoff、Proof 后续步骤和 Issue 后续步骤里只写 Claude 的 `/spec:work`。为什么要改：非 Claude 宿主会得到错误入口，但普通 prose 继续枚举宿主命令又会形成第二真相源。修复结论：经回审收敛为“当前宿主 work entrypoint”host-neutral 表述，不在普通 prose 中维护 Claude/Codex 命令对照。修复效果：`spec-plan` / `spec-write-tasks` handoff 不再绑定 slash-only 文案，contract 测试锁定不回归。验证目标：`npx jest tests/unit/spec-plan-contracts.test.js tests/unit/spec-write-tasks-contracts.test.js --runInBand`。
- 第 21 轮：发现 `spec-plan/SKILL.md` 主体仍有一份 post-generation 菜单摘要写着 `Start /spec:work`，与第 20 轮修订后的 reference 产生副本漂移。同步改为“Start work + 当前宿主 work entrypoint”，并补测试同时检查主 skill 与 reference。验证目标：`npx jest tests/unit/spec-plan-contracts.test.js --runInBand`。
- 第 22 轮：问题描述：`spec-work` 与 `spec-work-beta` 在 large bare prompt 与过大计划回退提示里只写 `/spec:brainstorm` / `/spec:plan`。为什么要改：非 Claude 运行时会得到错误入口，且命令映射不应散落在执行 workflow prose 中。修复结论：经回审收敛为当前宿主 brainstorm/plan entrypoint 的 host-neutral 表述，不在普通 prose 中枚举命令映射。修复效果：两个 work workflow 保留正确 handoff 语义，并用 contract 测试防止回到 slash-only 文案。验证目标：`npx jest tests/unit/spec-work-contracts.test.js tests/unit/spec-work-beta-contracts.test.js --runInBand`。
- 第 23 轮：问题描述：`spec-debug` 在“Rethink the design”选项、Smart escalation 表格和 Phase 3 跳转说明中只写 `/spec:brainstorm`。为什么要改：debug 只应表达转入 brainstorm workflow 的意图，不应绑定 Claude slash 命令。修复结论：经回审收敛为当前宿主 brainstorm entrypoint 的 host-neutral 表述。修复效果：debug 工作流可在 Codex 与后续宿主中复用同一语义边界。验证目标：`npx jest tests/unit/spec-debug-contracts.test.js --runInBand`。
- 第 24 轮：问题描述：`spec-optimize` 的 post-completion 选项只写 `/spec:code-review` 和 `/spec:compound`。为什么要改：优化结束后的 review/compound 是 workflow 选择，不应在 optimize prose 中复制宿主命令对照。修复结论：经回审收敛为当前宿主 code-review / compound entrypoint。修复效果：post-completion 菜单保留可执行意图，同时降低 host surface 漂移。验证目标：`npx jest tests/unit/spec-optimize-contracts.test.js --runInBand`。
- 第 25 轮：问题描述：`spec-compound` 的 Usage、lightweight mode 重跑提示、manual override 与 Related Commands 都绑定 Claude slash 入口。为什么要改：compound 是双宿主 workflow，但普通 skill usage 不应成为入口映射表。修复结论：经回审收敛为 current-host compound/plan entrypoint，Usage 改为 current-host 表述。修复效果：用户提示仍能表达要调用的 workflow，同时避免第二份 Claude/Codex 映射。验证目标：`npx jest tests/unit/spec-compound-contracts.test.js --runInBand`。
- 第 26 轮：问题描述：`spec-sessions` Usage 只展示 `/spec:sessions`。为什么要改：对非 Claude 宿主不准确，也会把 session workflow 的入口治理分散到 skill prose。修复结论：经回审收敛为 current-host sessions entrypoint，不在 session skill 中维护宿主命令对照。修复效果：session history skill 的使用说明跟随当前宿主入口，后续新增宿主不需要改这里。验证目标：`npx jest tests/unit/spec-sessions-contracts.test.js --runInBand`。
- 第 27 轮：问题描述：`spec-brainstorm` 主说明、通用收尾和 requirements 模板都把下一步固定为 `/spec:plan` / `/spec:brainstorm`。为什么要改：brainstorm 只应输出下一步 workflow 意图，具体命令由当前宿主入口治理负责。修复结论：经回审收敛为当前宿主 plan/brainstorm entrypoint 的 host-neutral 表述。修复效果：requirements handoff 不再 slash-only，并有 contract 测试防止回归。验证目标：`npx jest tests/unit/spec-brainstorm-contracts.test.js --runInBand`。
- 第 28 轮：问题描述：`spec-ideate` 的 Proof handoff recommended next step 固定为 `/spec:brainstorm`。为什么要改：会把非 Claude 用户引到错误入口，并把 Proof 后续步骤绑定到具体宿主命令。修复结论：经回审收敛为当前宿主 brainstorm entrypoint，不复制宿主命令映射。修复效果：ideation 结果可稳定交给当前宿主的 brainstorm workflow。验证目标：`npx jest tests/unit/spec-ideate-contracts.test.js --runInBand`。
- 第 29 轮：问题描述：`spec-plan/references/universal-planning.md` 在 non-software pipeline fallback 中只写 `/spec:plan`，并只禁止 `/spec:work`。为什么要改：非软件计划也应遵循当前宿主入口，同时“用户调用 `/spec:plan`”会把 plan workflow 锁死到 Claude 表达。修复结论：经回审收敛为当前宿主 plan/work entrypoint 的 host-neutral 表述，并把“用户调用 `/spec:plan`”改成 host-neutral “plan workflow”。修复效果：non-software planning 的入口和禁用 work 提示都不再依赖 slash command。验证目标：`npx jest tests/unit/spec-plan-contracts.test.js --runInBand`。
- 第 30 轮：继续复核 `spec-plan` handoff 文案，发现 Proof `localSynced: false` 分支仍用 `Start /spec:work` 指代菜单项，和前面改为 host-neutral `Start work` 的菜单不一致。改为 `Start work`，并补 contract 防止 stale-local 提示再次回到 slash-only 菜单名。验证目标：`npx jest tests/unit/spec-plan-contracts.test.js --runInBand`。
- 第 31 轮：问题描述：`spec-update` 的 marketplace 本地开发提示只说在 Claude Code 会话运行 `/spec:update`。为什么要改：`spec-update` 是双宿主 workflow，但普通 skill prose 不应维护第二套入口映射；中心入口表和 init 已负责宿主差异。修复结论：经回审改为“run the update workflow in a regular host session”。修复效果：更新检查提示不再绑定 Claude slash，也不复制 Claude/Codex 命令对照。验证目标：`npx jest tests/unit/spec-update-contracts.test.js --runInBand`。
- 第 32 轮：问题描述：`test-browser`、`feature-video`、`frontend-design` 和 `spec-polish-beta` 的 `agent-browser` 缺失提示使用“run `/spec:mcp-setup` in Claude or `$spec-mcp-setup` in Codex”这类二分文案。为什么要改：这些技能都是下游使用者，应引用 MCP setup 的当前宿主入口语义，而不是复制宿主分支。修复结论：统一改为“current host's MCP setup entrypoint”，不在普通 prompt 中列命令映射。修复效果：浏览器相关工作流的缺失依赖提示形成单一语义模式，降低 host surface 漂移。验证目标：`npx jest tests/unit/browser-helper-tool-contracts.test.js --runInBand`。
- 第 33 轮：问题描述：`spec-doc-review` 的 Invocation Boundary 已强调不是 agent type，但入口说明仍写成“Claude users call / Codex users call”的用户分支句式。为什么要改：workflow 边界的核心应是“当前宿主入口”，而不是把宿主分支散落在 prose 中。修复结论：改为“current host's document-review entrypoint”，不列具体宿主命令；嵌套 caller 仍在当前 orchestrator 内执行。修复效果：文档评审 workflow 的非 agent 属性和宿主适配层分离更清楚，也避免普通 skill 成为入口映射真相源。验证目标：`npx jest tests/unit/workflow-invocation-boundary.test.js --runInBand`。
- 第 34 轮：问题描述：按用户反馈回审本次 staged 的 `loop.md` 改动及第 20-33 轮相关源码/测试 diff，发现多处记录仍把“显式列出 Claude/Codex 命令映射”写成长期修复效果。为什么要改：当前 init/governance 已区分 Claude 和 Codex，普通 skill prose 继续枚举入口会制造第二真相源，未来支持 Cursor 时会扩大维护面。修复结论：保留中心入口治理中的映射，普通 workflow prose 回收为 current-host/host-neutral 表述，并补扫描测试禁止“current host entrypoint + `/spec`/`$spec` 映射”回流。修复效果：`loop.md` 记录、skill prose 与测试重新对齐“入口映射集中治理，workflow 只引用当前宿主入口”的边界；第 1-19 轮本次未改动，未纳入本轮事实重写。验证目标：`npx jest tests/unit/spec-plan-contracts.test.js tests/unit/spec-write-tasks-contracts.test.js tests/unit/spec-work-contracts.test.js tests/unit/spec-work-beta-contracts.test.js tests/unit/spec-debug-contracts.test.js tests/unit/spec-optimize-contracts.test.js tests/unit/spec-compound-contracts.test.js tests/unit/spec-sessions-contracts.test.js tests/unit/spec-brainstorm-contracts.test.js tests/unit/spec-ideate-contracts.test.js tests/unit/spec-update-contracts.test.js tests/unit/browser-helper-tool-contracts.test.js tests/unit/workflow-invocation-boundary.test.js --runInBand`。
- 第 35 轮：问题描述：README/README.zh-CN 的“当前范围”和“上下文与 Graph Readiness”共享段落只用 `$spec-mcp-setup`、`$spec-graph-bootstrap`、`$spec-plan` 等 Codex 命令描述主路径。为什么要改：README 是中心公开入口文档，允许维护双宿主映射，但共享段落若只写 Codex 会误导 Claude 用户，也会让后续宿主扩展读成 Codex-only。修复结论：将共享 setup、graph bootstrap、plan、work/review 路径改为 current-host 表述，并在首次定义处显式列出 Claude Code 与 Codex 的对应入口；保留后续 Workflow Entry Points 表作为完整映射。修复效果：README 顶部路径、runtime readiness 表和 workflow 映射表职责分离，用户先看到宿主无关主链，再到表格查具体入口。验证目标：`npx jest tests/unit/dual-host-governance-contracts.test.js --runInBand`。
- 第 36 轮：问题描述：README/README.zh-CN 的 Install 代码块连续列出 `spec-first init --claude` 与 `spec-first init --codex`，但没有说明这是按实际宿主选择运行。为什么要改：新用户可能误以为单宿主项目也必须同时初始化两个 runtime，增加不必要的生成资产和 source/runtime drift 认知负担。修复结论：在中英文 README 安装片段后补充“按实际使用宿主运行 init；只用单宿主就只跑对应 flag，需要双宿主时才两个都跑”。修复效果：安装路径更贴近真实使用场景，同时保留双宿主支持能力。验证目标：`npx jest tests/unit/dual-host-governance-contracts.test.js --runInBand`。
