# Fixture Prompt: docs-only contract clarification

Update `docs/contracts/workflows/example-run-artifact.md` so it clearly says the documented run artifact is planned and does not imply a current runtime producer.

Constraints:

- Do not edit runtime mirrors or generated assets.
- Update `CHANGELOG.md` using the repository format.
- Keep the change docs-only.
- Verify with the changelog format test and `git diff --check`.
