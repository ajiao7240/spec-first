{
  "schema_version": "v1",
  "fixture_id": "graph-degraded-fallback",
  "scenario_type": "graph-degraded-fallback",
  "prompt_path": "prompt.md",
  "repo_path": "repo",
  "expected_workflows": [
    "$spec-plan",
    "$spec-work",
    "$spec-code-review"
  ],
  "expected_changed_paths": [
    "docs/plans/fallback-plan.md",
    "CHANGELOG.md"
  ],
  "expected_artifacts": [
    {
      "path": "docs/plans/fallback-plan.md",
      "kind": "workflow-summary",
      "owner": "spec-plan"
    },
    {
      "path": ".spec-first/graph/provider-status.json",
      "kind": "workflow-summary",
      "owner": "graph-readiness"
    }
  ],
  "validation_commands": [
    "npx jest tests/unit/changelog-format.test.js --runInBand",
    "git diff --check"
  ],
  "quality_signals": [
    "Graph readiness is degraded and must be named as advisory evidence.",
    "The workflow must fall back to bounded direct reads instead of treating graph facts as confirmed truth."
  ],
  "degraded_mode_expectations": {
    "reason_code": "graph-provider-unavailable",
    "fallback": "bounded-direct-read",
    "evidence": [
      ".spec-first/graph/provider-status.json",
      "src/workflows/example.js"
    ]
  }
}
