// fixture: JS 基础函数与 import 语句
import { helper } from './utils';
import path from 'path';

function formatDate(date) {
  return date.toISOString();
}

const greet = (name) => {
  return `Hello, ${name}`;
};

function add(a, b) {
  return a + b;
}
